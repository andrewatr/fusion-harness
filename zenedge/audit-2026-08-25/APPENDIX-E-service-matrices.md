# Appendix E — Service, scheduler, core-plumbing and subsystem matrices

## LENS 3/6 — services slice 3 (every 3rd file of `ls services/*.py services/*/*.py | grep -v _pb2`)

**Coverage.** RESOLVED LIST (56 files, not 63 — `ls services/*.py services/*/*.py | grep -v _pb2` yields 168 files at a59646ae, and `awk 'NR%3==0'` selects 56):
services/rithmic_protocol/__init__.py, services/spotgamma_client/_log_safety.py, services/audit.py, services/spotgamma_client/cache.py, services/coach_conversation.py, services/coach_guards.py, services/drift_gate.py, services/equity_trust_certificates.py, services/flow_alignment_validator.py, services/flow_patrol_llm_extractor.py, services/flow_patrol_table_parser.py, services/gamma_read.py, services/gap_fill_grader.py, services/guardrail_cue_generator.py, services/guardrail_scorer.py, services/hiro_grading_engine.py, services/hiro_regime.py, services/import_price_verification.py, services/legacy_pnl_service.py, services/level_edge_service.py, services/ludwig_rebuild_detector.py, services/market_calendar.py, services/market_state.py, services/mindfulness_service.py, services/scott_regime/mtd_telemetry.py, services/option_pnl.py, services/option_verdict.py, services/playbook_aggregation.py, services/playbook_metrics_aggregator.py, services/playbook_primitive_validator.py, services/playbook_tier.py, services/prove_it.py, services/regime_vocab.py, services/risk_metrics_service.py, services/rulebook_grader.py, services/session_analysis.py, services/sg_token_runs.py, services/sierra_marketdata.py, services/spotgamma_context_store.py, services/spotgamma_llm_extractor.py, services/spotgamma_signal_recorder.py, services/stats_recomputer.py, services/strategy_touch_semantics.py, services/support_intake_ledger.py, services/telegram_notifier.py, services/ticker_profile_rollup.py, services/spotgamma_client/trace.py, services/trade_attribution.py, services/transcription_provider.py, services/tts_elevenlabs.py, services/user_playbook_service.py, services/v12_daily_brief.py, services/v12_session_analysis.py, services/verdict_v0_data.py, services/wellness_seed.py, services/educator/zoom_client.py.

Only 4 of the 12 priority names fall in this slice (sierra_marketdata, level_edge_service, guardrail_scorer, coach_guards); the other 8 (option_position_grouping, collective_pulse_service, gap_fill_confluence_engine, price_level_alerts, user_notifications, option_strategy_economics, verdict_v0, logical_trades) are NOT in the NR%3==0 selection and were not audited by me — another lens owns them.

DEPTH, honestly tiered:
- Read end-to-end (27): rithmic_protocol/__init__, _log_safety, audit, spotgamma_client/cache, coach_conversation, coach_guards, drift_gate, equity_trust_certificates, flow_alignment_validator, guardrail_cue_generator, guardrail_scorer, hiro_regime, import_price_verification, level_edge_service (all 1014 lines), market_calendar, option_verdict, playbook_aggregation, playbook_metrics_aggregator, playbook_primitive_validator, playbook_tier, regime_vocab, sg_token_runs, support_intake_ledger, transcription_provider, tts_elevenlabs, zoom_client, + sierra_marketdata read in full for lines 1-135, 547-935, 1098-1500, 1595-1621 (~65%).
- Read in targeted depth around grep hits (14): gap_fill_grader, hiro_grading_engine, legacy_pnl_service, market_state, mindfulness_service, mtd_telemetry, risk_metrics_service, rulebook_grader, telegram_notifier, ticker_profile_rollup, trade_attribution, v12_daily_brief, verdict_v0_data, wellness_seed.
- Docstring + pattern-grep only, NOT line-audited (15): flow_patrol_llm_extractor, flow_patrol_table_parser, gamma_read, ludwig_rebuild_detector, option_pnl (2322 lines — only the module-global Sierra-unavailable cache at :77-96 was inspected), prove_it, session_analysis, spotgamma_context_store, spotgamma_llm_extractor, spotgamma_signal_recorder, spotgamma_client/trace, stats_recomputer, strategy_touch_semantics, user_playbook_service, v12_session_analysis. These carry the highest residual risk in this slice; option_pnl.py in particular is unfinished.
- No generated code in this slice (protobuf already excluded by the grep -v _pb2).
Deliberately NOT re-reported per the known list: legacy_pnl_service daily-aggregate rounding and the 28-key symbol config; risk_metrics_service p_value/no-op monte-carlo; the eight tick registries; utils/safe_task and audit.py's documented logs-and-drops. All DB- and router-layer defects left to their waves (routers/hiro_regime.py:132 and routers/sierra_marketdata.py cited only as blast radius).

| module | role | writes what | tenant context | failure mode on error | verdict |
|---|---|---|---|---|---|
| sierra_marketdata.py | in-request market-data + excursion service | `analytics.trade_excursions` (runtime DDL) | none set; caller's conn; table has no user_id | swallow → 0 / "unavailable" | **DEVIATES** — F1,F2,F3,F4,F5,F6,F26,F28 |
| level_edge_service.py | in-request signal recorder + post-hoc evaluator | `sierra_chart.level_edge_signals` (runtime DDL) | takes AsyncSession; **dedup ignores user_id** | swallow → `{"signals":[],"count":0}` | **DEVIATES** — F7,F8,F9,F10,F11,F12,F25 |
| playbook_aggregation.py | post-response, own sync conn (`rls-skip`) | `logical_trades.suggested_playbook_id`, `playbook_id` | user_id only in the SELECT; UPDATEs are `WHERE id=%s` | try/except + traceback, returns counts | **DEVIATES** — F16,F17 |
| hiro_grading_engine.py | in-request trade grading | none (returns plan) | AsyncSession from caller | fabricated fallback ATR, log-only | **DEVIATES** — F13,F14 |
| playbook_metrics_aggregator.py | in-request, opens its OWN pool conns | none | sets `app.current_user_id` per task ✓ | gather→None per playbook | **DEVIATES** — F15,F18 |
| hiro_regime.py | pure classifier + static edge table | none | n/a | NO_DATA row carries win_rate 50.0 | **DEVIATES** — F19,F27 |
| sg_token_runs.py | scheduler breadcrumb (sync psycopg2) + health read | `sg_token_refresh_runs` | `rls-skip`, no tenant dimension ✓ | swallow, warn | **DEVIATES** — F20,F30 |
| transcription_provider.py | in-request ASR dispatch | none | n/a | retries then raises / whisper fallback | **DEVIATES** — F21 |
| flow_alignment_validator.py | pure cohort stats (anonymized rows) | none | features only, no user_id ✓ | none (pure) | **DEVIATES** — F22 |
| telegram_notifier.py | post-response notifier | none | chat_id from caller | returns False | **DEVIATES** — F23 |
| gap_fill_grader.py | pure grader | none | n/a | fabricates 0.0 probability | **DEVIATES** — F24 |
| mtd_telemetry.py | scheduled telemetry | telemetry rows | user_id/email passed explicitly | None on empty | **DEVIATES** — F29 |
| v12_daily_brief.py | in-request brief builder | none | user-scoped queries | 0 on empty | **DEVIATES** — F31 |
| guardrail_cue_generator.py | pure text templater | none | n/a | generic template | **DEVIATES** — F32 |
| regime_vocab.py | pure vocabulary normalizer | none | n/a | unknown tokens pass through (fail-closed) | **DEVIATES** — F33 |
| rulebook_grader.py | in-request grader | none | conn from caller | raises typed errors ✓ | **DEVIATES** — F34 |
| mindfulness_service.py | in-request CRUD/streaks | `mindfulness_sessions` | every query `WHERE user_id=$1` ✓ | raises | **DEVIATES** — F35 |
| guardrail_scorer.py | pure scorer | none | n/a | n/a (pure) | **DEVIATES** — F36 |
| option_verdict.py | pure verdict classifier | none | n/a | UNAVAILABLE only on all-null greeks | **DEVIATES** — F37 |
| zoom_client.py | outbound Zoom OAuth/REST | none | educator-scoped by caller | raise_for_status ✓ | **DEVIATES** — F38 |
| audit.py | in-txn + fire-and-forget audit writer | `audit_log` | `set_config(...,true)` in own txn ✓ | logs `[AUDIT] DROPPED`, swallows | conforms (documented, known) |
| legacy_pnl_service.py | in-request, own sync conns (`rls-skip`) | none (read-only) | user_id in every WHERE | ValueError/LookupError ✓ | conforms (defects already known) |
| risk_metrics_service.py | in-request risk maths | none | conn from caller | 0-defaults | conforms here (p_value/MC already known) |
| market_state.py | in-request regime snapshot | none | per-instrument, not per-user | 30s process-local TTL cache, per-dyno | conforms (cache bounded by instrument set) |
| market_calendar.py | pure NYSE calendar | none | n/a | raises on 10-day miss ✓ | conforms |
| drift_gate.py, playbook_tier.py, coach_guards.py, _log_safety.py, spotgamma_client/cache.py, support_intake_ledger.py, playbook_primitive_validator.py, equity_trust_certificates.py, coach_conversation.py, import_price_verification.py, tts_elevenlabs.py, rithmic_protocol/__init__.py | pure predicates / thin adapters / best-effort writers | see each | none needed or caller-supplied | typed exceptions or documented best-effort | conform |
| flow_patrol_llm_extractor.py, flow_patrol_table_parser.py, gamma_read.py, ludwig_rebuild_detector.py, option_pnl.py, prove_it.py, session_analysis.py, spotgamma_context_store.py, spotgamma_llm_extractor.py, spotgamma_signal_recorder.py, spotgamma_client/trace.py, stats_recomputer.py, strategy_touch_semantics.py, user_playbook_service.py, v12_session_analysis.py, trade_attribution.py, ticker_profile_rollup.py, wellness_seed.py | mixed | mixed | mixed | mixed | **NOT LINE-AUDITED** — grep/docstring only; no verdict claimed |

---

## LENS 4/6 — schedulers/ (67 files), jobs/ (4 files), worker.py — Heroku Scheduler crons + always-on worker loops, at /Users/zen/code/zenedge-backend-fusion @ a59646ae

**Coverage.** All 72 units ENUMERATED (header/docstring/monitor-construction pass on every file, plus a repo-wide grep census of ping_snitch / advisory-lock / date-key / weekend-gate call sites). DEEP-READ end-to-end (main + failure paths + SQL): worker.py (all 1726 lines), utils/scheduler_monitoring.py, utils/scheduler_trace.py, utils/scheduler_locks.py, utils/dms_config.py (lists), and 34 schedulers: aggregate_daily_ib, audit_log_retention, backfill_daily_levels(tail), check_notifications, daily_gap_update, emini_addict_gapfill_sync(tail+freshness gate), evaluate_signals, firing_detector_tick, forward_validator_nightly, gamma_touch_evaluate(tail), globex_open_gapfill_alert, gsst_evaluate(query), gsst_sync(tail), hiro_snapshot, ibb_auto_detect, ibb_evaluate, level_edge_evaluate, materialize_options_verdicts, menthorq_gex_sync(main), morning_brief_generate, outcome_resolver_tick, overnight_coaching_precompute, overnight_voice_processing(tail), rebuild_rithmic_groupings(main), regime_alignment_check(tail), regime_cell_stats_recompute(main), regime_snapshot(tail), rithmic_evening_import, run_confluence(tail), run_gamma_density(tail), scott_playbook_repair, scott_regime_matview_refresh, spotgamma_data_health(main), spotgamma_evaluate, stats_recomputer_nightly, sync_sg_universe, tier_classifier_nightly, zdpsb_evaluate(query) + all 4 jobs/. SAMPLED (docstring + targeted greps for snitch/date/weekend/LIMIT patterns, not line-by-line): backfill_synth_oi, backfill_trace, count_orphaned, deploy_drift_alert, ensure_sierra_partitions, hiro_divergence_evaluate, hiro_flatline_evaluate, hiro_flatline_stats_sync, idempotency_cleanup, match_transcriptions_v3, menthorq_qscore_sync, query_performance_snapshot, recall_partition_maintenance, reprocess_transcriptions, sg_producer_heartbeat, slo_budget_report, slo_weekly_report, spotgamma_flow_alignment_sync/_validate, spotgamma_flow_patrol_sync, spotgamma_founders_sync, spotgamma_memory_refresh, spotgamma_ticker_profile_narrative, spotgamma_token_refresh, strategy_lab_sre_probe, strategy_level_touch_ledger, tigerdb_health_sample. NOT VERIFIABLE FROM THE REPO: actual Heroku Scheduler cadences (the free add-on has no CLI and the dashboard is not committed — every cadence below is the file's own docstring claim, which spotgamma_flow_patrol_sync.py:36-44 documents has been wrong by 75 minutes before), and which DMS env vars are actually provisioned. No DB was queried; schema claims come from db/schema_test_seed_sierra.sql and db/migrations_sierra/. Generated protobuf: none in this slice.

| module | trigger (docstring claim) | writes | tenant ctx | on error | verdict |
|---|---|---|---|---|---|
| **worker.py** `import_rithmic_trades` | loop, 300s | `logical_trades`, `raw_trades`, `user_rithmic_config.last_import_time` | service-role, per-user advisory lock inside single-account import | broad `except` → 60s sleep → continue | **DEVIATES** — no snitch ping at all (`import_monitor` at :139 only used for Slack at :723/:741); intraday path always fetches yesterday (L4-01) |
| **worker.py** `process_pending_excursions` | loop, 60s | `pending_excursions`, `logical_trades.mfe/mae_ticks` | service-role | per-trade `except` → reschedule; cycle `except` → 60s | **DEVIATES** — error path never enforces `EXCURSION_MAX_ATTEMPTS` (L4-14); snitch pinged every cycle regardless of per-trade errors |
| **worker.py** `monitor_sierra_ohlc_freshness` | loop, 1800s | none (read-only) | service-role | `except` → Slack + `ping_snitch_strict(False)` | CONFORMS — model implementation of the strict-snitch idiom |
| **worker.py** `firing_detector_tick_loop` | loop, 600s | via `services.firing_detector.tick()` | service-role | `except` → Slack(30m cooldown) → 60s | conforms; lazy import at :1368 sits outside the guard (L4-13) |
| **worker.py** `deploy_drift_loop` | loop, 600s | `deploy_drift_alert_state`, `stale_pr_alert_state` | service-role | `except` → Slack → 600s | conforms; import at :1321 outside guard (L4-13) |
| **worker.py** `process_pending_option_materializations` | loop, 15s | option verdict/economics substrate | service-role | broad `except` → 60s | **DEVIATES** — no monitor/snitch at all; missing `return` when kill-switch off (:1415-1418) so it spins a no-op loop forever |
| **worker.py** `process_pending_transcriptions` / `mine_transcription_corrections` / `refresh_phonetic_cache` / `educator_pipeline_loop` | loops, 15s/300s/3600s/cfg; first two default OFF | `transcription_jobs`, `phonetic_mappings`, `educator_recordings` | service-role (explicitly cross-tenant) | per-job + per-cycle `except` | conform; no snitches (all gated off or best-effort) |
| **worker.py** `main()` | Procfile `worker:` | — | — | `asyncio.gather` **without** `return_exceptions=True` | **DEVIATES** — one loop's escape tears down 11 siblings (L4-13) |
| **evaluate_signals.py** | cron (cadence undocumented) | `sierra_chart.ibb_signals` outcome/pnl | service-role | none — first exception aborts the whole run | **DEVIATES** — resolves mid-session as "session close" (L4-03); zero bars → LOSS (L4-04); hardcoded `*4` ticks/pt for all symbols (:121-125) |
| **ibb_evaluate.py** | 10 min, RTH | `ibb_signals` | service-role | none per-signal | **DEVIATES** — bars query has no instrument/contract filter (L4-02) |
| **zdpsb_evaluate.py** | 10 min, RTH | `zdpsb_signals` | service-role | none per-signal | **DEVIATES** — same missing filter (:106-112, L4-02) |
| **level_edge_evaluate.py** | 10 min, RTH | `level_edge_signals` | service-role | run_traced_job → Slack + exit 1 | **DEVIATES** — queries nonexistent column `minute_utc` (L4-05) |
| **ibb_auto_detect.py** | 10 min, 10:30-16:00 ET | `ibb_signals` (INSERT) | service-role | per-symbol `except` → log → "completed successfully" | **DEVIATES** — total detection failure ⇒ green snitch (L4-16) |
| **gsst_evaluate.py** | 10 min | `gsst_signals` | service-role | run_traced_job | conforms (bars query correctly filtered by symbol, :105-113) |
| **gamma_touch_evaluate.py** | 10 min | `spotgamma_level_touches`, level memory | service-role | per-instrument `status=="error"` → warn only | **DEVIATES** — partial failure ⇒ green snitch (L4-16) |
| **spotgamma_evaluate.py** / **hiro_divergence_evaluate.py** / **hiro_flatline_evaluate.py** | 10 min | signal outcome tables | service-role | re-raise → run_traced_job | conform |
| **scott_playbook_repair.py** | every 4h | `logical_trades.playbook_id` | service-role, Scott only | n/a | **DEVIATES** — body unreachable behind `return` at :248, still pings DMS (L4-06) |
| **morning_brief_generate.py** | 14:20 UTC Mon-Fri | `morning_feeds` (user-visible) | service-role + per-user GUC on write | per-user `except` → count + Slack | **DEVIATES** — empty precompute ⇒ silent green (L4-07) |
| **overnight_coaching_precompute.py** | 07:00 UTC | `pre_computed_coaching` | service-role + per-user GUC | per-user `except` → count + Slack | **DEVIATES** — all-users-failed and no-users-found both ⇒ green (L4-07); unbounded per-user loop, no LIMIT/cursor (L4-20) |
| **spotgamma_data_health.py** | 23:00 UTC | none (read-only checks) | service-role | Slack on failures | **DEVIATES** — 6/6 checks failing still pings green (L4-08) |
| **globex_open_gapfill_alert.py** | 23:05/22:05 UTC | Slack alert (user-visible) | service-role | ERROR statuses → exit 1 | **DEVIATES** — DST breaks the 17:30-18:30 ET self-gate; SKIPPED pings green (L4-09) |
| **daily_gap_update.py** | ~21:30 UTC | `gap_fill_statistics` | service-role | `process_date` returns None on no-data | **DEVIATES** — zero rows written ⇒ green, no alert (L4-11) |
| **hiro_snapshot.py** | 21:05 UTC | `hiro_daily_snapshot` | service-role | run_traced_job | **DEVIATES** — "RTH" window is the whole UTC day (L4-12); check-then-INSERT, no ON CONFLICT |
| **rebuild_rithmic_groupings.py** | hourly + 23:30 UTC `--force` | `logical_trades`, `raw_trades.logical_trade_id` | service-role | errors → Slack, run returns 0 | **DEVIATES** — kill-switch-off and error paths both ⇒ green; no lock shared with the 23:00 importer (L4-18) |
| **rithmic_evening_import.py** | 23:00 UTC | via `worker.run_import_for_user` | service-role, env-pinned user list | raises after all users ⇒ Slack + exit 1 | CONFORMS — best-built job in the slice (weekend + `is_trading_day()` holiday gate, correct CME date) |
| **backfill_daily_levels.py** | daily | `spotgamma_daily_levels` | service-role | `success_count==0` → fail | **DEVIATES** — 1-of-3 symbols ⇒ green + "loaded" Slack (L4-16) |
| **aggregate_daily_ib.py** | 16:00 UTC | `sierra_chart.ib_daily_stats` | service-role | per-date `except` → collect | **DEVIATES** — `errors` non-empty + any success ⇒ exit 0 ⇒ green (:541, L4-16); auto-heal window capped at 20 days |
| **menthorq_gex_sync.py** / **menthorq_qscore_sync.py** | 14:30 / 21:30 UTC | `menthorq_daily_gex`, `qscore_daily` | service-role | `ping_snitch_strict(fail_count==0)` | CONFORM — correct strict gate; self-documented ZEN-688 gap (fallback-date hits count as success) remains |
| **emini_addict_gapfill_sync.py** | ~22:00 UTC | `emini_addict_gapfill` | service-role | freshness/future-date guards raise | CONFORMS — has a real staleness gate (:613-640) |
| **regime_snapshot.py**, **run_confluence.py**, **run_gamma_density.py**, **overnight_voice_processing.py** | daily | regime/zone/voice tables | service-role | partial failure → Slack + exit 1, no ping | CONFORM |
| **stats_recomputer_nightly.py** → **tier_classifier_nightly.py** → **forward_validator_nightly.py** | 05:00 / 05:10 / 05:20 UTC | `sg_rulebook_stats`, tiers, `sg_forward_validation` | service-role via `get_db()` | advisory lock each (3 *different* IDs) | **DEVIATES as a chain** — ordering enforced only by a 10-min wall-clock gap (L4-15) |
| **outcome_resolver_tick.py**, **firing_detector_tick.py** | 10 min | firings/outcomes | service-role | run_traced_job | conform (firing_detector's missing lock is already-known) |
| **regime_cell_stats_recompute.py** | 05:30/06:30 UTC | `regime_cell_stats` | service-role, per-user rows | advisory lock; `sys.exit(0)` on lock miss | **DEVIATES (minor)** — lock-miss `sys.exit(0)` is a BaseException that bypasses the snitch ping (L4-19); `if not trades: return` ⇒ green |
| **materialize_options_verdicts.py** | ~08:00 UTC | `option_position_verdicts` | service-role, advisory lock, fail-closed DMS gate | run_traced_sync_job | CONFORMS |
| **sync_sg_universe.py** | 05:00 UTC | `public.instruments` | service-role | empty-manifest ⇒ abort without writing | CONFORMS — best empty-payload guard in the slice |
| **scott_regime_matview_refresh.py** | 10 min | matview refresh | service-role | exit 1 on failure | minor — `httpx.get` snitch result ignored, a 4xx reads as a heartbeat (:117-121) |
| **audit_log_retention.py** | daily | DELETE from `audit_log` | service-role | run_traced_job | **DEVIATES (minor)** — unbounded `while True` drain, no total cap / time budget (L4-19) |
| **idempotency_cleanup.py**, **recall_partition_maintenance.py**, **query_performance_snapshot.py**, **ensure_sierra_partitions.py**, **check_notifications.py**, **count_orphaned.py** | hourly/daily/monthly | housekeeping only | service-role | run_traced_job / explicit | CONFORM |
| **backfill_synth_oi.py**, **backfill_trace.py**, **reprocess_transcriptions.py**, **refetch_specific_date.py** | manual one-off, no snitch | SG/transcription/trade tables | service-role | per-date skip-and-log | conform (documented idempotent, ON CONFLICT DO NOTHING, dry-run default) |
| **spotgamma_founders_sync.py** | 13:00+13:30 / 17:30+18:30 UTC | `spotgamma_market_context` | service-role | `self_heal_failed_rows()` each run | conforms — self-documented risk that a failing catch-up run overwrites a good primary row (:34-40) |
| **spotgamma_flow_patrol_sync.py**, **_alignment_sync.py**, **_alignment_validate.py**, **spotgamma_memory_refresh.py**, **spotgamma_ticker_profile_narrative.py**, **spotgamma_token_refresh.py**, **sg_producer_heartbeat.py**, **tigerdb_health_sample.py**, **strategy_lab_sre_probe.py**, **strategy_level_touch_ledger.py**, **slo_budget_report.py**, **slo_weekly_report.py**, **deploy_drift_alert.py**, **gsst_sync.py**, **hiro_flatline_stats_sync.py**, **match_transcriptions_v3.py** | daily/weekly/10-min per docstring | SG dossiers, SLO reports, ledgers, stats | service-role throughout | run_traced_job/sync | conform on the sampled dimensions; `gsst_sync` swallows per-trade errors (:481-483) and still reports success (L4-16 family) |
| **jobs/secret_age_monitor.py** | daily | none (Slack only) | n/a | blanket `except` + always `sys.exit(0)` | **DEVIATES** — no snitch, silent on Slack failure (L4-17) |
| **jobs/group_bookmap_fills.py** | DORMANT, not scheduled | `logical_trades` | service-role | per-user `except` → returns `{0,0}` | dormant; unbounded (no LIMIT on either query) |
| **jobs/refetch_specific_date.py** | manual `heroku run` | via import pipeline | service-role | CLI | conforms |
| **schedulers/__init__.py**, **_path_setup.py** | n/a | n/a | n/a | n/a | inert |

---

## LENS 5/6 — CORE PLUMBING: utils/ (40), middleware/ (3), app/ (3), models/ (217), database.py, main.py, router_registry.py, config/ (4) in /Users/zen/code/zenedge-backend-fusion @ a59646ae

**Coverage.** ENUMERATED (full listing obtained, every file accounted for): utils/ 40, middleware/ 3, app/ 3 (incl. app/settings/spine.py), config/ 4, models/ 217 (166 top-level + 51 across trades/imports/compliance/hiro/spotgamma/scott_regime), plus database.py, main.py, router_registry.py.

READ IN FULL (61 files): main.py, router_registry.py, database.py (1192L), all 3 middleware, utils/{auth,auth_middleware,auth_guard,deps,canonical_identity,token_blocklist,cache,redis,rate_limit,resilience,timezone,datetime_utils,currency_calculations,tick_sizes,instruments,symbol_mapping,market_sessions,json_helpers,error_helpers,error_responses,env_flags,environment,user_provisioning,scheduler_locks,security_headers_middleware,safe_task}, config/__init__.py (734L), app/settings/spine.py, models/{_base,_types,_constraints,current_user,user_account_risk,trades/*,imports/rithmic,imports/sierra_data}, plus targeted reads of routers/{logical_trades,streak_metrics,risk_metrics,playbooks,videos_cloudfront} to confirm rate-limiter blast radius.

MECHANICALLY SCANNED, NOT READ LINE-BY-LINE: the remaining ~200 model files. I ran structural greps across all 217 (base-class census: 651 BaseDTO / 377 BaseDomainModel / 3 plain BaseModel / 0 BaseEvent; extra= census; constraint census: only 18/217 files use max_length, 57/217 use ge/le/gt/lt; 251 `: str = Field(` and 152 `: list[...]` declarations carry no length bound) and read the shared type/base layer that governs all of them. I did NOT read every individual money/quantity model, so the per-field bound audit below is a census + representative deep-dive (models/user_account_risk.py), not an exhaustive per-model verdict.

NOT COVERED / DEFERRED: utils/{contract_mapper,futures_option_roots,sierra_api,slack_notify,gapfill_slack_notifier,orphan_slack_notifier,scheduler_monitoring,scheduler_trace,dms_config(partial),disclaimer_*,aws_credentials(partial),user_context(known)} were opened only partially or greppped. worker.py and zenedge_drill.py are out of slice. No generated protobuf files exist in this slice. Nothing was executed; no DB, no tests, read-only throughout.

CONFIDENCE DISCIPLINE: middleware execution order was derived from Starlette's documented add_middleware semantics (insert at index 0; list[0] becomes outermost) applied to main.py's literal add order — I did not run the app to observe it, but the ordering claim is deterministic from the source. PgBouncer-dependent findings (F6, F17) are marked INFERRED because I could not read the deployed DATABASE_POOL_URL; the code's own comments ("Required for PgBouncer transaction mode", ZEN-1583 at database.py:418-421) are the evidence that transaction pooling is live.

| module | role | writes what | tenant context | failure mode on error | verdict |
|---|---|---|---|---|---|
| **middleware/request_id.py** | outermost user MW (after connection_debug) | response header + `scope["state"]["request_id"]` + contextvar | none needed | n/a (no failure path) | DEVIATES — echoes unvalidated, unbounded client `X-Request-Id` into logs and the response header (l.29,38) |
| **middleware/deprecation.py** | 3rd from outer; only `/api/sierra2/` | sunset headers | reads `request.state.user` — but runs OUTSIDE AuthMiddleware, so state is always empty | n/a | DEVIATES — l.52-53 user enrichment is dead code; the log always says "unknown" |
| **utils/auth_middleware.py** | 4th; the JWT gate | `scope["state"]["user"]`, `["user_id"]`, `user_id_var` | ESTABLISHES the identity everything downstream uses | 401 emitted directly, bypassing CORS/GZip/SecurityHeaders | DEVIATES — F5 (hand-rolled CORS defaults to localhost:3000), two fail-open branches (l.310-312, 324-326) |
| **middleware/idempotency.py** | 5th; INSIDE auth, OUTSIDE gzip | `idempotency_keys` rows | reads user from scope state; DB via `get_db()` (service-role, no tenant GUC) | fail-open on DB error (l.289-293) | DEVIATES — F1, F2, F3, F4. Also l.14 `# rls-skip: middleware runs before request auth context` is factually wrong; it runs after |
| **utils/security_headers_middleware.py** | 6th | 6 response headers | none | n/a | DEVIATES — sits INSIDE idempotency, so replayed/409/422 idempotency responses carry no security headers |
| **GZipMiddleware** (main.py:296) | 7th | compresses body | none | n/a | DEVIATES — comment at l.295 claims "outermost"; it is second-innermost. Root cause of F1 |
| **CORSMiddleware** (main.py:276) | INNERMOST | CORS headers | none | n/a | DEVIATES — F5. Added first ⇒ innermost; cannot decorate any outer middleware's response or the global 500 handler |
| **utils/auth.py** | JWT validate + `get_current_user` | none | produces `CurrentUser.sub` = the RLS tenant key | 401 for both bad creds AND Auth0 outage | DEVIATES — F15 (optional variant swallows everything), F16 (no stale-JWKS fallback) |
| **utils/deps.py** | S3/OpenAI singletons + RBAC gates | none | `require_admin` returns a caller id string | 503 on open breaker; 401 on no creds | DEVIATES — F9. `require_support_permission` is correctly narrow; `require_admin` is not |
| **utils/auth_guard.py** | `{user_id}` path-param ownership | none | reads `request.state.user`; looks up `users.id` via `get_db()` | 401/403 fail-closed | DEVIATES (minor) — returns silently when the path has no `user_id` param (l.47-49); applied router-wide on legacy_trades/stats/pnl, so any route there taking the id as a query param is ungated |
| **utils/rate_limit.py** | abuse control | in-memory `_HITS` / Redis counters | keyed on `request.client.host` only — never the user | 429, or silent no-op | DEVIATES — F7, F8. Also no proxy-header trust configured anywhere (Procfile has no `--forwarded-allow-ips`), so all clients share the Heroku router's IP bucket |
| **utils/token_blocklist.py** | revocation | Redis keys | keyed by jti / sub | fail-open by design (l.66,74,86,93) | CONFORMS to its documented contract, but `is_token_blocked` only fires when the JWT carries `jti` (auth_middleware.py:302-303); Auth0 access tokens routinely omit it |
| **utils/cache.py** | Redis response cache | Redis values | key is caller-supplied; helper offers NO user-scoping affordance | all Redis errors swallowed to warnings | CONFORMS (documented); note `cache_or_fetch_json` `json.dumps(..., default=str)` (l.122) makes a Decimal serialize as a JSON string on a HIT but a number on a MISS — single caller today (services/trace_surface_defects.py:207) |
| **utils/redis.py** | pool | none | none | yields None ⇒ every caller degrades silently | CONFORMS |
| **utils/resilience.py** | retry + breaker | `_breakers` global dict | none | fast-fail | DEVIATES — F14. Also `get_circuit_breaker` silently ignores threshold/timeout args for an already-registered name (l.234-239) |
| **utils/currency_calculations.py** | ticks→dollars | none | none | returns 0.0 / {} | DEVIATES — F10 |
| **utils/instruments.py** | tick value | none | none | returns `default=5.0` (l.81) | DEVIATES — a third disagreeing unknown-instrument default ($5.00) alongside currency_calculations' $0.00 and the known logical_trades.py $1.00. Registry disagreement itself is on the known list |
| **utils/tick_sizes.py** | tick size | none | none | returns 0.25 default (l.122) | CONFORMS to the known-registry issue; no new defect |
| **utils/timezone.py** + **utils/datetime_utils.py** | time normalization | none | none | silent UTC fallback | DEVIATES (minor) — two same-named `to_utc_dt` with divergent empty-string behavior (None vs ValueError); `to_user_tz` silently coerces any non-whitelisted IANA zone to UTC (l.163-165) |
| **utils/market_sessions.py** | session windows | none | none | fails toward "open" | CONFORMS (limits documented; the calendar duplication is on the known list) |
| **utils/scheduler_locks.py** | cron mutual exclusion | advisory locks | service-role | exits cleanly if not acquired | DEVIATES — F17 |
| **utils/safe_task.py** | fire-and-forget | none | none | logs and drops | KNOWN — not re-reported; but see F18 for the shutdown-drain half |
| **utils/user_context.py** | demo routing | `_effective_user_cache` global | substitutes tenant | — | KNOWN — not re-reported |
| **utils/environment.py / env_flags.py / json_helpers.py / error_helpers.py / error_responses.py / user_provisioning.py / security_headers** | small helpers | — | — | documented defaults | CONFORM |
| **database.py — get_db()** | PRIMARY pooled API | any | `SET app.service_role='on'` SESSION-level, RESET on exit | re-raises `asyncio.TimeoutError` (no 503) | DEVIATES — F6; also the only pool entry point that does not convert exhaustion into a 503 (contrast l.464-470, 514-520, 654-660) |
| **database.py — get_db_rls_dep** | 83 routers | tenant tables | txn-local `current_user_id` + `service_role='off'` | 503 | CONFORMS — the correct pattern |
| **database.py — get_db_dep** | 2 routers | shared tables | NONE — no tenant GUC, no service-role marker | 503 | DEVIATES (bounded) — only routers/options_strategy_templates.py:31 and routers/trade_import/routes.py:376; under Stage-2 fail-closed RLS both silently see zero rows |
| **database.py — get_db_service_dep / service_role_txn** | webhooks, jobs | any | txn-local `service_role='on'` + audit log | 503 | CONFORMS — the correct pattern |
| **database.py — get_main_db / get_sierra_db** | SQLAlchemy engines | any | NONE | raises | DEVIATES (latent) — `get_main_db` has zero callers in routers/services/schedulers; both lazily init a module-global engine with no lock (l.959, l.1072), so a concurrent first-request can build and orphan a second 15-connection pool |
| **database.py — pool config** | l.241-336 | — | — | `init_db_pool` raises; caller in main.py swallows | DEVIATES (minor) — `init_sierra_pool` declares `acquire_timeout` (l.742) but never assigns it and then logs it (l.800) as if it applied; Sierra silently reuses the main pool's `_acquire_timeout` global (l.840) |
| **database.py — SSL** | l.303-304, 778-779, 965-966, 1078-1079 | — | — | — | DEVIATES (accepted-risk class) — `check_hostname=False` + `CERT_NONE` on every non-localhost DB connection incl. the primary |
| **main.py — lifespan startup** | l.76-139 | pools, registries | — | pool failures logged then re-raised by `verify_database_connections`; redis/instrument-registry/phonetic failures are non-fatal warnings | CONFORMS on DB; DEVIATES softly — a failed `registry.load_from_db()` (l.112-116) silently activates the hardcoded tick tables with only a WARNING |
| **main.py — lifespan shutdown** | l.143-158 | — | — | closes pools immediately | DEVIATES — F18 |
| **router_registry.py** | mounts 18 groups | routes | — | collects `failed`, logs ERROR, boots anyway | DEVIATES — F13 (duplicate sg_poc import l.19/23 and call l.50/53). Boot-continues-on-failure is intentional but means a missing router group is a log line, not a boot failure |
| **config/__init__.py** | Settings singleton at import (l.729) | — | — | `Settings.initialize()` raises ⇒ import fails ⇒ hard boot failure | CONFORMS on validation-runs-at-startup; DEVIATES — F20, F21. No secret carries a non-empty default; `ZENEDGE_SIGNING_KEY` (l.167) is the sole unredacted one. Every setting is import-time-only (restart required). Numeric settings (poll intervals, timeouts, budgets) carry no ge/le bounds |
| **app/settings/spine.py** | 2nd settings singleton at import (l.71) | — | — | `_positive` validator on 6 ints | DEVIATES — F19. Also has no `env_file`, unlike config.Settings, so local `.env` SPINE overrides are silently ignored |
| **config/multi_account_beta.py, rithmic_config.py, rithmic_symbol_map.py** | static maps | — | — | — | CONFORM (enumerated, not deep-read) |
| **models/_base.py** | 3 base classes | — | — | BaseDTO `extra="forbid"`, BaseDomainModel `extra="ignore"`, BaseEvent `extra="allow"` | CONFORMS — discipline is real: 651/1031 classes are BaseDTO (forbid), only 3 plain BaseModel, and `BaseEvent` (the `extra="allow"` one) has **zero** subclasses. This hunt item came back clean |
| **models/_types.py** | shared constrained aliases | — | — | — | DEVIATES — F11. Also `RiskPercent` caps at 0.10 (fraction) while user_account_risk.py caps the same concept at 100 (percent) — two conventions, neither enforced on the other |
| **models/_constraints.py** | `MAX_PLAYBOOK_ID` from env at import | — | — | — | CONFORMS |
| **models/current_user.py** | the tenant-key carrier | — | `sub` feeds every RLS GUC | pydantic 422→401 | DEVIATES (minor) — `sub: str` has no max_length/pattern despite `Auth0UserId` existing in _types.py for exactly this |
| **models/user_account_risk.py** (money deep-dive) | account risk DTOs | risk settings | — | 422 | DEVIATES — F22. Bounds ARE present on `account_size` (gt=0, le=10M) and strings (max_length) — this file is among the better-constrained ones, which is what makes the dead validator notable |
| **models/{trades,imports,compliance,hiro,spotgamma}/*.py** — 49 files | claimed backward-compat shims | — | — | — | DEVIATES — F12 |
| **models/** — remaining ~200 files | DTOs/domain models | — | — | — | CENSUS ONLY: 18/217 files use `max_length`; 57/217 use numeric bounds; 251 unbounded `str` Fields and 152 unbounded collection Fields. Not individually verified — reported as a census, not as per-model findings |

---

## LENS 6/6 — analytical & integration subsystems: agent/, sierra/, scotts_zones_detector/, ml/, backtest/, cluster_hunter/, gamma_density/, observability/, ludwig/, tws/, monitoring/, evals/ (+ one cross-cut note on rithmic-microservice/)

**Coverage.** ENUMERATED (file listing + import-graph reachability from main.py/worker.py/routers) for all 12 named directories, so the coverage matrix rows are complete at module granularity. READ IN FULL OR NEAR-FULL: backtest/{gsst_stair_backtest,gsst_stair_walker,gsst_evaluator,gsst_primary,models,constants,economics}.py; gamma_density/{atr_fetcher,persistence}.py + gamma_density/bifurcation/{engine,backtest,validation,walkforward}.py; agent/v12/coaching_jobs.py + agent/v12/mcp_tools/market_tools.py; scotts_zones_detector/{db_persistence,aplus_order_executor,message_parser}.py; ml/trade_quality/{model,features}.py + the v6_candidates/v6_verification behavioural-lookup blocks; observability/slo.py; evals/flow_patrol/{assertions.js,promptfooconfig.yaml}. SAMPLED BY TARGETED GREP ONLY (findings from these are marked INFERRED or rest on single lines I read directly): the 44-file agent/ v2–v11 pile + agent/legacy/ (8 files) — I read only trading_agent_framework_v9.py's DB blocks; agent/v12/{orchestrator,daily_brief,day_of_week_report,report_generator,pattern_evolution,memory,claude_runner,agent_loader,tracing,cli}.py; agent/v12/coaching/agents/* (10 files, unread); agent/v13/*; sierra/production-pipeline/* (40 files — I read only menthorq_v2_import.py's tz argument and the ludwig/indicators divergence); ludwig/{touch_detector,touch_evaluator,statistics}.py; tws/{tws_service,streaming_service}.py; cluster_hunter/{collectors,persistence}; scotts_zones_detector/{main,price_monitor,game_board,tui_*,telegram_*,analyze_detections,detection_logger}.py; ml/{gpr_edge_gate/*, trade_quality/{training*,evaluate,market_state,aplus_reversion_combo,v3_honest_eval,v6_heroku_verify}}.py; observability/{metrics,logging,tracing,context}.py. NOT DONE: no dynamic verification of any kind (read-only, no psql, no test run), so every claim about production DATA (e.g. whether ludwig_levels rows are actually 7–8h skewed, whether menthorq_v2 level columns are per-minute or forward-filled) is a code-level claim only. Schema claims are against db/schema_test_seed_sierra.sql + db/migrations_sierra/, not a live catalog. I did not re-derive the eight known tick registries; new registry locations are reported as new locations only. Generated protobuf (rithmic-microservice/proto/*_pb2.py) skipped per instruction.

| module | role / entry point | what it computes | who consumes | writes what | tenant context | failure mode on error | verdict |
|---|---|---|---|---|---|---|---|
| `backtest/` (gsst_primary, gsst_stair_*, gex*, ludwig_*, put_support, call_resistance) | `routers/gsst_stair_backtest.py`, `routers/gsst_walkforward.py` | level-touch / stair-progression win rates, continuation matrix, expectancy | GSST backtest + walkforward API | `sierra_chart.gsst_signals` (gsst_primary.store_signals_to_db) | **none** — `user_id` literal `"backtest"` (gsst_models.py:205); no GUC | per-row `except` → `logger.error`, loop continues, caller told "stored N" | **DEVIATES** — F8 (non-atomic swallowed writes), F5 (evaluator off-by-one), F6 (RTH filter dead clause), F14 (expectancy from `touches[0]` R:R) |
| `backtest/economics.py` + `constants.py` | library | ticks/point, $/tick, futures vs equity_etf P&L | all backtest strategies | — | n/a | raises `UnknownInstrumentError` — **no silent default** | **CONFORMS** — the one registry in this lens that fails loud on an unknown symbol |
| `gamma_density/bifurcation/` (engine, backtest, walkforward, validation) | `routers/gamma_density_bifurcation.py` | ATR-dynamic vs static zone thresholds, hit/touch rates, walk-forward recommendation + "confidence" | bifurcation API (user-facing) | none (read-only) | none — instrument/date keyed, global | per-day/per-period `except` → `continue`, zeroed row, aggregate still returned | **DEVIATES** — F1 (validation query names a column that does not exist → every hit rate is a fabricated 0.0), F2 (dynamic threshold from `NOW()` ATR), F11 (TIE counted as a correct prediction; confidence can exceed 1.0) |
| `gamma_density/{collector,clustering,persistence,config,atr_fetcher}.py` | library under the bifurcation engine | level collection, tick threshold, zone clustering | bifurcation engine | `sierra_chart.gamma_density_zones` (DELETE-then-INSERT, single psycopg2 txn, commit on clean exit) | none — instrument/trade_date keyed, global table | exception → no commit → conn.close() rolls back | **CONFORMS on the write path** (atomic); `atr_fetcher.get_latest_atr` is the source of F2 |
| `agent/v12/mcp_tools/market_tools.py` | `routers/spotgamma_chat.py`, coaching agents | HIRO / ATR+RelVol / TICK / MenthorQ / Ludwig "at trade entry" | v12 coaching, chat, ML feature builders | — | none (market data) | returns `None` on any exception; agents treat as "missing context" | **DEVIATES** — F4 (three mutually inconsistent nearest-bar predicates; one reaches +5 min past the entry instant), F16 (new tick registry, silent default 4) |
| `agent/v12/coaching/data_fetcher.py` | v12 orchestrator | per-user trades, risk profile, personality, market context | v12 coaching session analysis | — | every query filters `WHERE user_id = $1` (spot-checked lines 398, 625, 751, 852, 1450, 1556, 1687) | `asyncio.gather(..., return_exceptions=True)` → error dicts with `*_available: False` | **DEVIATES** — `fetch_hiro_latest()` (line 898) takes no date and hits `/api/spotgamma/latest`; feeds F3 |
| `agent/v12/antipattern_detector.py` | v12 orchestrator (`orchestrator.py:484`) | per-trade antipatterns + 0–100 risk score | coaching report shown to the user | — | inherits the caller's user scope | no error path — emits whatever it can | **DEVIATES** — F3 (today's live HIRO judged against a past session's trades), F13 (`set()` ordering decides which recommendations are shown) |
| `agent/v12/coaching_jobs.py` | `routers/v12_coaching.py` | job lifecycle for the ~60 s coaching analysis | coaching poll endpoints | in-process dict only | owner check `job.user_id != user_id` (correct) | job absent → 404 "not found, expired, or belongs to another user" | **DEVIATES** — F12 (process-local store behind `gunicorn -w 4`) |
| `agent/v13/{deterministic_engine,narrative_wrapper,models}.py` | `scripts/v13_haiku_demo.py` only | deterministic coaching metrics, LLM narrative | nothing in the app | — | `user_id` parameter threaded | per-system `_compute_system` catches → `SystemResult(error=...)` | **STALE** — no router/worker reachability; error isolation itself is sound |
| `agent/*.py` (44 top-level) + `agent/legacy/` (8) | none | v2–v11 coaching frameworks | each other only | `agent/trading_agent_framework_v9.py` opens `DATABASE_URL` directly | **none — hardcoded `auth0\|67cb…e1`** | prints and continues | **DEVIATES** — F19 (dead pile, no live-version marker), F20 (hardcoded real user id) |
| `ml/trade_quality/{model,features}.py` | `routers/trade_quality_v3.py`, `routers/ml_reversion_combo.py` | P(win), RED/YELLOW/GREEN, "confidence" | trade-quality API | — | none (model artefacts) | `predict` raises only if the model is unloaded | **DEVIATES** — F9 (grade emitted on wholly missing features; confidence is a restatement of p), F15 (duplicated predict path), F17 (UTC-day grouping for `daily_pnl_before`) |
| `ml/trade_quality/{v6_candidates,v6_verification}.py` | offline training/verification | walk-forward CV, OOF calibration | model selection | model artefacts on disk | n/a | abort gates | **CONFORMS on leakage** — prior trades filtered by `exit_time_utc < entry_time_utc` (v6_candidates.py:103, v6_verification.py:85); the UTC-day grouping caveat is F17 |
| `ml/gpr_edge_gate/` | `routers/trade_quality_v3.py` (`TwoStageGate`) | second-stage edge gate | trade-quality API | model artefacts | n/a | not read | **UNVERIFIED** — sampled by grep only |
| `sierra/ludwig_import_pipeline.py` | operator CLI | Ludwig 5-min bars + 14 levels | `sierra_chart.ludwig_levels` → `market_tools.fetch_ludwig_levels`, `backtest/ludwig_*.py` | `sierra_chart.ludwig_levels` (batched, `ON CONFLICT DO NOTHING`, commit per 10k) | n/a (market data) | `except` → `conn.rollback()` — but earlier chunks are already committed | **DEVIATES** — F7 (localises naive Sierra timestamps to US/Pacific while every sibling importer treats them as UTC) |
| `sierra/import_sierra_indicators.py`, `import_sierra_1s.py`, `production-pipeline/menthorq_v2_import.py` | operator CLI | 5-min indicators, 1-s bars, MenthorQ v2 | `sierra_indicators_5min`, `sierra_ohlc_1sec`, `menthorq_v2` | those tables | n/a | per-chunk commit | **CONFORMS to each other** (all UTC; menthorq exposes `--source-tz`, default `UTC`) — they are the majority F7 disagrees with |
| `sierra/production-pipeline/*` (40 further importers) | operator CLI | per-instrument OHLC/ATR/RelVol/internals | sierra DB | sierra tables | n/a | not read | **UNVERIFIED** — sampled by grep only |
| `scotts_zones_detector/aplus_order_executor.py` | `routers/aplus_control.py`; `tws/tws_order_service.py` | arms/blocks live TWS bracket orders, tracks fills, computes P&L ticks | A+ control API | none (TWS orders) | global, single-operator by design | order blocked → `logger.info` + `return None` | **DEVIATES** — F10 (`/arm` and `/disarm` report a stale import-time snapshot), F18 (new tick registry, silent 0.25 default); docstring claims a paper-trading default while the code defaults to the live port |
| `scotts_zones_detector/{db_persistence,message_parser}.py` | telegram forwarder (`worker.py:37`), `routers/aplus_control.py` | parses Scott's zone messages, persists the game board | A+ TUI/API | `scotts_zones_game_board` (single upsert per call) | **no tenant column and no GUC** — deliberately global | `except` → `logger.error`, returns `None`/`False` | **DEVIATES** — F14b (micro contracts normalise to junk roots "ME"/"MN"/"M2") |
| `scotts_zones_detector/zone_attribution.py` | routers ×8, `worker.py:495` | attributes imported trades to zones | trade import | zone attribution rows | **KNOWN DEFECT — not re-reported** | — | out of scope per brief |
| `cluster_hunter/` (13 files) | none | confluence-zone clustering + score | nothing | `sierra_chart.confluence_zones`, `confluence_run_log` (writer.py) — unreachable | none | `print()` diagnostics | **STALE** — declared in `pyproject.toml:91` but zero importers; duplicates `gamma_density/clustering.py` with no marker of which is live (F19) |
| `ludwig/` | `routers/price_alerts.py` imports `constants.TICKS_PER_POINT` only | touch detection/evaluation/statistics | nothing | — | n/a | — | **PARTIALLY STALE** — `touch_detector`/`touch_evaluator`/`statistics` have no importer outside `ludwig/__init__.py` |
| `observability/` | `main.py`, `worker.py`, 15 routers | request-id context, structured logging, Prometheus metrics, SLO budgets | `routers/slo_status.py`, scrapers | Prometheus gauges | request-scoped, not tenant-scoped (correct) | gauges silently keep their last value | **DEVIATES** — F10b (error budget reads 1.0 right up to the breach) |
| `tws/` | `services/gap_fill_confluence_engine.py`, `scotts_zones_detector/{price_monitor,live_atr,aplus_order_executor}` | IB market data + bracket orders | A+ executor, gap-fill engine | TWS orders | global | not read in depth | **UNVERIFIED** — only the port defaults were read (`tws_order_service.py:130` = 7497 paper) |
| `monitoring/` (tigerdb_monitor.py, ohlc_canary.sh) | operator CLI | TimescaleDB cost/CPU rules engine | operator | `~/.zenedge/tigerdb_history.jsonl` | n/a | documented exit codes 0–4, cadence guard | **CONFORMS** — the best-disciplined module in this lens |
| `evals/flow_patrol/` | `npx promptfoo eval` | extraction completeness / symbol recall / numeric grounding | model bake-off | none | n/a | deterministic JS assertions | **CONFORMS** — temperature/top_p/seed pinned, corpus + tests.json git-frozen, objective gold from a deterministic parser |
| `rithmic-microservice/` (skipped per brief) | Fly.io, `Dockerfile` CMD → `app_protobuf_v4:app` | Rithmic order/fill capture | main app import path | none directly | none | duck-typed field probing | **DEVIATES** — F21 (second, independently-written decoder; `app.py`/`simple_app.py` are dead siblings) |

---

## LENS 1/6 — services/ SLICE 1 (every third file, NR%3==1) in /Users/zen/code/zenedge-backend-fusion @ a59646ae

**Coverage.** The prompt said 63 modules; the deterministic rule `ls services/*.py services/*/*.py | grep -v _pb2 | awk 'NR%3==1'` resolves to 56 files at a59646ae. That is my slice and I read every one of them (26,715 lines total). Resolved list, in awk order: services/__init__.py, scott_regime/__init__.py, account_risk_scanner.py, scott_regime/behavioral.py, scott_regime/classifier.py, coach_eval.py, coach_turn.py, entity_extraction.py, firing_detector.py, flow_patrol_enrich.py, flow_patrol_merge.py, forward_validator.py, gamma_touch_evaluator.py, gsst_analysis.py, guardrail_feedback.py, guardrail_signal_extractor.py, hiro_live.py, historical_regime.py, instrument_registry.py, legacy_stats_service.py, llm_provider.py, ludwig_rebuild_storage.py, market_data_freshness.py, educator/market_window.py, spotgamma_client/models.py, option_greeks.py, option_position_grouping.py, outcome_resolver.py, playbook_auto_assign.py, playbook_phonetic_map.py, playbook_statistics.py, preflight_gate.py, qwen_llm_provider.py, rithmic_protocol/repository_client.py, risk_resolver.py, spotgamma_client/scheduler.py, sg_analysis.py, sierra_contract_lifecycle.py, scott_regime/significance_whitelist.py, spotgamma_email_ingest_service.py, spotgamma_memory_calculator.py, spotgamma_token.py, strategy_lab_sre.py, streak_analysis.py, support_intake_policy.py, ticker_profile_context.py, tier_classifier.py, trace_surface_builder.py, rithmic_protocol/trade_collector.py, transcription_queue.py, spotgamma_client/universe.py, v12_agent_brief.py, v12_feedback.py, v12_shared.py, rithmic_protocol/websocket_client.py, zdpsb_analysis.py. FULLY READ line-by-line (40): coach_eval, forward_validator, transcription_queue, qwen_llm_provider, v12_feedback, scott_regime/__init__, spotgamma_client/universe, flow_patrol_merge, ticker_profile_context, educator/market_window, scott_regime/significance_whitelist, guardrail_feedback, v12_agent_brief, ludwig_rebuild_storage, preflight_gate, historical_regime, flow_patrol_enrich, tier_classifier, playbook_statistics, guardrail_signal_extractor, gsst_analysis, support_intake_policy, rithmic_protocol/repository_client, v12_shared, playbook_auto_assign, account_risk_scanner, risk_resolver, streak_analysis, outcome_resolver, hiro_live, firing_detector, sierra_contract_lifecycle, gamma_touch_evaluator, sg_analysis, option_greeks, instrument_registry, llm_provider, zdpsb_analysis, spotgamma_token (key paths), spotgamma_memory_calculator (key paths). READ IN LARGE PART, structurally scanned with targeted reads of every arithmetic/fallback/time/state site (16): entity_extraction, playbook_phonetic_map, scott_regime/behavioral, scott_regime/classifier, coach_turn, trace_surface_builder, market_data_freshness, legacy_stats_service, option_position_grouping, strategy_lab_sre, spotgamma_client/scheduler, spotgamma_client/models, spotgamma_email_ingest_service, rithmic_protocol/trade_collector, rithmic_protocol/websocket_client, services/__init__.py (empty). For those 16 I state role/failure-mode confidently but a finding could remain in prose I skimmed rather than read — I have marked the two findings that rest on that reading as INFERRED. I additionally read non-slice files ONLY as corroborating evidence for a slice finding (stats_recomputer.py:141, routers/playbooks.py:315, routers/streak_metrics.py:197, routers/user_playbook_stats.py:244, legacy_pnl_service.py:29, agent/v12/coaching/persistence.py:141, agent/v12/mcp_tools/market_tools.py:372, schedulers/match_transcriptions_v3.py:66). Protobuf *_pb2.py excluded by the slice rule as instructed — none examined. No mutations: read-only tools only, no app/test/psql runs. Skipped-per-instructions (present in my slice but on the ALREADY-KNOWN list, noted in the matrix, not re-reported): option_greeks.py:44 DEFAULT_RISK_FREE_RATE, option_position_grouping.py:876/1059 int(multiplier), gsst/zdpsb/scheduler instrument-registry duplication, strategy_lab_sre + market_data_freshness weekday-only calendar.

| module | role | writes what | tenant context | failure mode on error | verdict |
|---|---|---|---|---|---|
| streak_analysis.py | in-request streak metrics (v5/v6 + demo) | nothing (read) | caller conn (`get_db_rls_dep`); `user_id=None` path drops the filter entirely | returns zero-valued defaults w/ `datetime.now()` timestamps | **DEVIATES** — F1 (v6 momentum/recovery/quality metrics are hardcoded constants), F2 (`playbook_id` accepted+logged, never applied to SQL), `pnl_ticks>=0`=win here vs `>0` in playbook_statistics, `DATE(entry_time)` (session TZ) in the v6 query vs `DATE(entry_time AT TIME ZONE 'UTC')` in the v5 query |
| playbook_statistics.py | in-request per-playbook aggregation for /stats/ticks, /ticks_analysis, /playbook_win_rate | nothing (read) | explicit `user_id = $1` on every query | SQL exception propagates to router | **DEVIATES** — F3 (buy/sell counts fabricated as trade_count×0.5), F4 (PF=0 for an all-winning playbook; both intended branches dead), F5 (confidence score caps at 0.21), F6 (win-rate definition + units differ between two functions in this file), F7 (`exit_time <= end_date::date` drops the final day and every open trade) |
| legacy_stats_service.py | in-request legacy win-rate/expectancy/PF | nothing (read) | `user_id` int passed to every helper; `rls-skip: legacy cross-user analytics` | ValueError on bad range; KeyError on instrument outside instrument_config | **DEVIATES** — F8 (profit factor multiplied by tick_value → dimensionally wrong), F9 (PF = avg_win when loss_rate==0), F10 (win_rate counts winning DAYS while win_rate_logical_trade_basis counts TRADES) |
| gamma_touch_evaluator.py | scheduled touch detection + HELD/FAILED resolution | `gamma_level_touches`, `gamma_level_memory` (global market data) | own `asyncpg.connect(SIERRA_DATABASE_URL)`; Sierra DB has no user rows — OK | run_evaluation re-raises; fetch helpers return [] | **DEVIATES** — F11 (EITHER levels can never FAIL; forced resolution defaults HELD), F12 (touch window excludes deep breaks → survivorship bias), F13 (`COALESCE(reaction_component, 50)`), F14 (touch_date derived two different ways), also resolves the same-bar ambiguity toward HELD while outcome_resolver resolves it toward LOSS |
| spotgamma_memory_calculator.py | daily job scoring SpotGamma level "memory" | `spotgamma_level_history`, `spotgamma_level_memory` | own connection; global market data — OK | raises on missing SIERRA_DATABASE_URL | **DEVIATES** — F15 (45% of memory_strength_score is fabricated: reaction is an appearance-count placeholder, consistency is the constant 100.0; a level seen once today scores ≈70 → "HIGH") |
| account_risk_scanner.py | in-request multi-account risk scan for V12 coaching | nothing (read) | own `get_db_connection()`, explicit `user_id=$1 AND account=$2`; `rls-skip` | **returns `[]`** (= "no violations") on any exception | **DEVIATES** — F16 (`tick_value = 50` for every instrument; registry says ES is 12.50/tick), F17 (SIZE_CREEP_THRESHOLD reused as the oversize tolerance), F18 (silent `[]` on failure), Kelly win_rate denominator includes scratches while avg_loss excludes them |
| risk_resolver.py | resolve account/workspace/system risk settings | nothing (read) | own connection, explicit user_id; `rls-skip` | **returns `system_defaults()`** ($2,500 / 1%) on any exception | **DEVIATES** — F19 |
| zdpsb_analysis.py | pure ZDPSB rule grading + dollar opportunity cost | nothing (pure) | n/a | no guards; parses numbers back out of display strings | **DEVIATES** — F20 (RTH grade uses the raw UTC hour, labels it ET), F21 (DOLLARS_PER_POINT default 50.00 → YM 10×, MYM 100× overstated) |
| outcome_resolver.py | ~10-min scheduled WIN/LOSS/TIMEOUT resolution | `sg_rulebook_outcomes` | service-role conn supplied by caller; `rls-skip` | per-firing isolation, counters, never aborts | **DEVIATES** — F22 (ZEN-490 sentinel TIMEOUT reaches stats_recomputer unfiltered), F23 (partial 1sec coverage resolves without gap detection) |
| firing_detector.py | ~10-min scheduled drift+trigger evaluation | `sg_rulebook_firings` | `get_db()` with `app.service_role='on'`; cross-user by design | per-primitive/per-instrument isolation | **DEVIATES** — F24 (empty `recent_closes` silently reverts to the superseded spot-snapshot predicate), F25 (`approach=ANY` → hardcoded "long") |
| historical_regime.py | entry-time regime reconstruction for baseline grading | nothing (read) | `get_sierra_pool_conn()`; global market data | raises NoRegimeSnapshotError when gamma backbone missing (good) | **DEVIATES** — F26 (vol ratio 1.0 → fabricated NORMAL), F27 (no staleness floor on the SPX/VIX as-of reads) |
| preflight_gate.py | in-request pre-flight gate verdict | nothing (read) | Sierra AsyncSession; market-wide data | never raises; always returns a populated response | **DEVIATES** — F28 (empty TRUTH_TABLE → NO_TRADE for every input, stamped `data_source="live"`) |
| sg_analysis.py | pure SG rule grading + Sierra fetch helpers | nothing (service layer) | Sierra AsyncSession; market data | safe_float → None; graders return score=50 | **DEVIATES** — F29 (unknown persisted gamma_regime → POSITIVE), F30 (3 of 5 rules return score=50/passed=True on absent data), F31 (naive datetimes bound to timestamptz, ±5min lookahead, UTC-date vs ET-date for the same daily_levels table) |
| v12_shared.py | shared V12 enrichment/assembly helpers | nothing (service layer) | user_id threaded to memory tools | market/user context failures logged as "non-critical" | **DEVIATES** — F32 (`alignment_score: 100` hardcoded), F33 (`user_risk_ticks = 12` fabricated); also `instrument else "ES"`, `int(secs/60)` truncation, UTC-date session grouping |
| spotgamma_token.py | SG JWT decode/refresh/mint/persist | Heroku config vars (via PATCH) | n/a (service credential) | classified RefreshResult; raises when token dead + no key | **DEVIATES** — F34 (httpx exceptions logged verbatim on requests bearing the SG JWT and the Heroku write-protected key, while universe.py:57-61 in the same package guards the identical hazard) |
| rithmic_protocol/websocket_client.py, repository_client.py | Rithmic WS login / agreements | none (external) | n/a (broker credentials) | reconnect attempts; unbounded `while True: recv()` in repository_client:167 | **DEVIATES** — F35 (`check_hostname=False` + `CERT_NONE` on credential-bearing TLS at websocket_client.py:58-59 and repository_client.py:72-73) |
| market_data_freshness.py | pure freshness/gap read-model for operator tooling | nothing (pure) | n/a | fallback calendar heuristics | **DEVIATES** — F36 (unknown producer table can never reach stale/actionable; daily_levels "fresh" from key presence with no age check). Weekday-only calendar fallback = ALREADY KNOWN, not re-reported |
| llm_provider.py | Flow Patrol multi-provider LLM ladder | nothing (returns dicts) | n/a | escalate/retry; returns best or `{"error": ...}` | **DEVIATES** — F37 (openai/anthropic clients get no timeout or max_retries while kimi/glm/local do), F38 (`complete_json_ladder` returns a gate-FAILED draft indistinguishable from a passing one; `_count_success` fires on a rung that failed the gate) |
| instrument_registry.py | boot-time singleton instrument specs | nothing | `rls-skip`: non-tenant reference data | `load_from_db` swallows everything, keeps hardcoded table | **DEVIATES** — F39 (tick_value/tick_increment/ticks_per_point fabricate ES values for unknown symbols; latent — no in-repo caller today) |
| flow_patrol_enrich.py | post-extraction LLM fill of hollow symbol rows | nothing (mutates dict) | n/a | any failure leaves extraction unchanged | **DEVIATES** — F40 (prompt mandates non-null inferred bias/conviction/dealer_positioning with no provenance flag, unlike the `_numeric_source` flag its sibling sets) |
| flow_patrol_merge.py | merge parser numbers over LLM numbers | nothing (returns copy) | n/a | any failure returns extraction unchanged | **DEVIATES** — F41 (`row[fld] = r.get(fld)` overwrites a good LLM value with None when the parser row lacks the field) |
| spotgamma_client/scheduler.py | SG polling loops → Sierra persistence | 8 sierra_chart tables | Sierra pool; global market data | per-insert try/except → False → snitch suppressed | **DEVIATES** — F42 (`_parse_trade_date` falls back to today's UTC date; vx `sample_time` falls back to now) |
| v12_agent_brief.py | in-request multi-agent coaching brief | `coaching_sessions` via safe_create_task | user_id passed through | ImportError/RuntimeError raised; persistence non-blocking | **DEVIATES** — F43 (`instrument or "ES"` on the upsert key `(user_id, session_date, instrument)`) |
| v12_feedback.py | in-request feedback submission | `user_feedback` | ownership checked in Python; helper opens its own `get_db()` | ValueError/LookupError/PermissionError | **DEVIATES** — F44 (`score` accepted and sent to LangSmith but never persisted locally) |
| ludwig_rebuild_storage.py | store detected rebuild events | `sierra_chart.ludwig_rebuilds` | global market data | batch → per-row fallback | **DEVIATES** — F45 (returns `len(rebuilds)` under a docstring promising "excludes duplicates") |
| playbook_auto_assign.py | post-match playbook auto-assignment (cron) | `logical_trades.playbook_id` | psycopg2 service-role conn, `rls-skip`; the UPDATE carries **no user_id predicate** | returns (False, reason) | **DEVIATES** — F46 |
| guardrail_signal_extractor.py | pure phrase-match risk signals on transcripts | nothing (pure) | n/a | returns [] when no trading context | **DEVIATES** — F47 (`raw_snippet` stores the phonetically-preprocessed text, not the raw transcript) |
| hiro_live.py | single choke point for SpotGamma HIRO reads | nothing (read) | Sierra conn or self-acquired pool; market data | StaleError / None / CorruptColumnError — honest | **DEVIATES** — F48 (`latest_hiro(asof=None)` without `reference_now` always reports `age_seconds=0, is_stale=False`; latent — no in-repo caller). Otherwise the strongest module in the slice |
| educator/market_window.py | webinar-window market overlay | nothing (read) | Sierra via market_tools | returns `{"coverage": False}` — honest | **DEVIATES** — F49 (`atr` field carries TICKS; the `atr_14` fallback is dead — no such attribute on the result type) |
| gsst_analysis.py | pure GSST rule grading | nothing (pure) | n/a | none | **DEVIATES** — missing stop/target → `score=50, passed=True`; `"per $50K account"` hardcoded in a user-facing recommendation; 2/5/10-point tolerances applied identically to ES and NQ. Folded into F30's class; reported there as the sibling instance |
| option_position_grouping.py | import-path option leg grouping + strategy upsert | `logical_trades` leg cols, `options_strategies` | user_id on every write | best-effort per-leg; economics → (None,None,None) | MOSTLY CONFORMS — one deviation: `_resolve_leg_asset_class_and_multiplier` returns the equity 100 for a futures-option root when the registry lookup fails, i.e. exactly the "phantom 100 multiplier" its own docstring exists to prevent (INFERRED trigger: registry miss). `int(multiplier)` at 876/1059 = ALREADY KNOWN |
| option_greeks.py | pure BS/BS2002 pricing, IV, Greeks, attribution | nothing (pure) | n/a | NaN on arb violation; 0.0 at intrinsic — explicitly refuses to fabricate | CONFORMS (best-in-slice). Minor: `greeks()` accepts `style` and always returns European Greeks (documented). Risk-free-rate constant = ALREADY KNOWN |
| forward_validator.py | nightly forward-test comparator | `sg_forward_validation` | service-role conn; `rls-skip` | per-pair isolation, counters | CONFORMS. Correctly filters `bars_to_resolve > 0` — the only consumer that does (see F22). Cosmetic: `classify_forward` always returns `n_outcomes=0` |
| tier_classifier.py | nightly tier assignment | `user_playbook_definitions.tier` | service-role conn; `rls-skip` | per-primitive isolation | CONFORMS |
| scott_regime/classifier.py | pure gate-state classification | nothing (pure) | n/a | `wilson_lcl` returns None (not 0.0) at n=0 — deliberately | CONFORMS |
| scott_regime/significance_whitelist.py | pure S4 demotion gate | nothing (pure) | n/a | demotion-only; env kill-switch | CONFORMS |
| scott_regime/__init__.py | universe constants | nothing | n/a | n/a | CONFORMS |
| sierra_contract_lifecycle.py | contract-lifecycle manifest load/build | manifest file (atomic, 0600) | n/a | fails CLOSED with an explicit reason | CONFORMS. Minor: `parse_contract_code` maps a 1-digit year to 200X (public API; all in-repo call sites pre-normalize) |
| support_intake_policy.py | pure intake safety gates | nothing (pure) | n/a | deny-by-default | CONFORMS |
| coach_turn.py | in-request coach turn (STT→LLM→guards→TTS) | coach turn rows via router | user_id checked in `_resolve_trade`/`_resolve_session` | distinct replies for LLM-fail vs STT-miss; deterministic guard floor | CONFORMS. Minor: `_session_snapshot` swallows to `logger.debug`, so a failed snapshot silently removes streak context |
| coach_eval.py | golden-set eval harness (tests/CLI only) | nothing | n/a | raises on missing fixtures | CONFORMS |
| entity_extraction.py | phonetic/entity extraction from transcripts | nothing (pure + cache reads) | per-user overlay threaded | returns has_prefix False / empty lists | CONFORMS. Note: `confidence: 0.95` is a constant for an exact regex match, and it clears playbook_auto_assign's 0.90 `high_confidence` bar |
| playbook_phonetic_map.py | static phonetic→playbook tables | nothing | n/a | n/a | CONFORMS |
| scott_regime/behavioral.py | GHEA behavior classification | nothing (returns models) | conn passed in | per-query try/except | CONFORMS |
| trace_surface_builder.py | TRACE surface assembly from dataframes | nothing (returns models) | conn/dataframe passed in | empty-frame guards, `generated_at` tz-aware UTC | CONFORMS |
| strategy_lab_sre.py | read-only SRE probe over both DBs | nothing (read) | service-role conns | per-check CheckResult, never raises | CONFORMS. Weekday-only session predicates = ALREADY KNOWN (firing_detector.py:101 explicitly compensates with `is_trading_day`) |
| spotgamma_email_ingest_service.py | persist SG email extractions | `spotgamma_*` ingest tables | Sierra session | upsert on conflict | CONFORMS |
| spotgamma_client/models.py | SG DTOs | nothing | n/a | pydantic defaults (`0.0` on signal fields — populated only from parsed payloads) | CONFORMS |
| spotgamma_client/universe.py | SG covered-symbol manifest | nothing | n/a | returns `[]` **and logs**, callers instructed to abort; uses `safe_exc_summary` | CONFORMS — the correct pattern F34 is missing |
| guardrail_feedback.py | record cue feedback / link outcomes | `guardrail_feedback`, `guardrail_outcome_links`, `guardrail_cues` | own `get_db()`, explicit user_id on the ownership check; `rls-skip` | returns False/0 on exception | CONFORMS (borderline: `record_feedback` returning False cannot distinguish "not your cue" from "DB down") |
| ticker_profile_context.py | assemble SG dossier for prompt grounding | nothing (read) | psycopg2 conn passed in; global market data | returns `{has_data: False}` | CONFORMS |
| transcription_queue.py | enqueue durable transcription job | `transcription_jobs` | user_id column written; conn passed in | idempotent upsert | CONFORMS |
| qwen_llm_provider.py | cached local-Qwen client factory | nothing | n/a | inert unless flag+URL set; timeout set | CONFORMS |
| rithmic_protocol/trade_collector.py | Rithmic trade collection | none (in-memory) | n/a | per-trade try/except | CONFORMS (inherits F35 via websocket_client) |
| services/__init__.py | empty | nothing | n/a | n/a | CONFORMS |

---

## LENS 2/6 — services slice 2 (`ls services/*.py services/*/*.py | grep -v _pb2 | awk 'NR % 3 == 2'`), worktree /Users/zen/code/zenedge-backend-fusion @ a59646ae

**Coverage.** The command resolved to 56 files, not 63 (the modulo-2 stripe of 168 non-protobuf service files). Resolved list, in order: services/educator/__init__.py, spotgamma_client/__init__.py, apns_push.py, scott_regime/cache.py, spotgamma_client/client.py, coach_fact_sheet.py, collective_pulse_service.py, equity_marketdata.py, flow_alignment.py, flow_patrol_fetch.py, flow_patrol_store.py, gamma_memory_calculator.py, gap_fill_confluence_engine.py, guardrail_context_builder.py, guardrail_pipeline.py, hiro_flatline_service.py, hiro_momentum.py, ibb_analysis.py, legacy_calculations.py, legacy_trades_service.py, logical_trades.py, macro_fred.py, market_regime.py, rithmic_protocol/message_handler.py, scott_regime/mtd_placeholder.py, option_materialization.py, option_strategy_economics.py, phonetic_rules.py, playbook_detection_simple.py, playbook_preprocessing.py, playbook_tap_matcher.py, price_level_alerts.py, regime_cell_stats.py, scott_regime/rigor.py, rithmic_service.py, secrets_vault.py, sg_substrate_ref.py, sierra_data.py, spotgamma_chart_levels.py, spotgamma_email_parser.py, spotgamma_signal_evaluator.py, spotgamma_touch_detector.py, strategy_level_touch_ledger.py, streak_metrics.py, spotgamma_client/synth_oi.py, ticker_profile_narrative.py, touch_outcome_tracker.py, trace_surface_defects.py, scott_regime/trade_gate_matrix.py, trigger_phrase_detection.py, user_notifications.py, v12_coaching.py, v12_patterns.py, verdict_v0.py, rithmic_protocol/websocket_client_protobuf.py, zenedge_insights.py.

NOTE: only 4 of the 17 named priority modules fall in this stripe (legacy_calculations, sierra_data, secrets_vault, streak_metrics) — the other 13 (instrument_registry, risk_metrics_service, legacy_stats_service, option_greeks, firing_detector, outcome_resolver, rulebook_grader, spotgamma_token, spotgamma_client/scheduler, historical_regime, playbook_statistics, audit, drift_gate) are NOT in this stripe and were not audited here.

Read in full (every line): 44 modules. Read partially (docstring + constants + all public entry points + every arithmetic/DB/except site I could locate by grep, but not every line): collective_pulse_service.py (read ~600/959), gamma_memory_calculator.py (~350/958), hiro_flatline_service.py (~450/1157), gap_fill_confluence_engine.py (~450/1256), user_notifications.py (~250/1190), ibb_analysis.py (~350/794), option_materialization.py (~450/616), rithmic_service.py (~470/598), logical_trades.py (~350/1319), price_level_alerts.py (read in full), strategy_level_touch_ledger.py (full). NOT opened at all: services/rithmic_protocol/websocket_client_protobuf.py (326 lines — protobuf transport wrapper, skipped as adjacent to generated code) and services/educator/__init__.py (1 line, empty). Two zero-content modules (educator/__init__.py, scott_regime/rigor.py re-export) and two pure facades (v12_coaching.py, spotgamma_client/__init__.py) carry no logic.

Cross-cutting context established once and reused: database.get_db() sets `SET app.service_role='on'` (database.py:406) so RLS is BYPASSED for every background module in this slice — isolation there depends entirely on explicit user_id predicates, which I checked per query. database.get_db_connection_sync() (database.py:1121-1143) sets NEITHER a tenant GUC NOR the service marker, so under the Stage-2 restrictive policy (db/migrations/20260709140000_rls_failclosed_stage2_cutover.sql) any RLS-covered table it reads returns zero rows; I could not confirm from the repo whether the legacy `trades` table carries those policies (no migration creates or policies it), so I did not raise that as a finding.

Two candidate findings were verified DEAD and downgraded rather than dropped silently: services/legacy_calculations.py has no production caller (only tests import its models), and services/streak_metrics.py's four cross-user CTE constants have no caller either. Both are reported with that stated in blast_radius.

Not re-reported per the exclusion list: the eight tick registries per se (I report only one NEW location where a wrong default is arithmetically load-bearing), MAE/MFE double-computation, the 28-key aggregate_pnl config, logical_trades.py:362/371 and :903/938, option_position_grouping int(multiplier), utils/safe_task, utils/user_context, worker.py:347, the two market calendars, option_pnl risk-free rate, and all router/DB-layer defects.

| module | role | writes what | tenant context | failure mode on error | verdict |
|---|---|---|---|---|---|
| price_level_alerts.py | in-request (TWS bar ingest) | alert_level_touches, user_notifications, level_touch_outcomes | **hardcoded `ALERT_USER_ID` default (:52)**; touch table not user-keyed | returns [] / False (fail-open dedup) | **DEVIATES** (F1, F14, F15, F19) |
| streak_metrics.py | in-request | none (read) | `compute_playbook_streak_analysis` filters user_id ($4); **4 module CTEs do not** | returns import-time-stamped default object | **DEVIATES** (F2, matrix note) |
| legacy_trades_service.py | in-request | none (read) | user_id param; own psycopg2 conn, no GUC | raises → 500, or per-instrument `continue` | **DEVIATES** (F3, F17, F18) |
| ibb_analysis.py | in-request | ibb analysis rows | user_id params, conn injected | raises ValueError on thin IB | **DEVIATES** (F4) |
| spotgamma_signal_evaluator.py | scheduler | sierra_chart.spotgamma_signals outcome/pnl | non-tenant (sierra); own asyncpg conn | leaves PENDING | **DEVIATES** (F5, F13, F23) |
| sierra_data.py | in-request | none (read) | conn injected | per-trade 0.0/0.0 result | **DEVIATES** (F6, F7) |
| flow_patrol_store.py | scheduler | spotgamma_flow_patrol(+_symbols) | non-tenant | commits a status='failed' doc | **DEVIATES** (F8, F26) |
| hiro_flatline_service.py | in-request + scheduler | hiro_flatline_signals | user_id for prefs only; stats are global | returns baseline/50% | **DEVIATES** (F9, F10) |
| gamma_memory_calculator.py | scheduler | gamma_memory_levels | non-tenant | logs, partial | **DEVIATES** (F11) |
| macro_fred.py | in-request | none | non-tenant | per-release `continue` → empty calendar | **DEVIATES** (F12, F25) |
| collective_pulse_service.py | in-request | regime snapshots | **conn is `get_db_rls_dep` → user-scoped** while copy says "ZenEdge traders" | ml_stats → zeros | **DEVIATES** (F16, F27) |
| market_regime.py | in-request | none | non-tenant (sierra) | per-series omit; vix_state → "steady" | **DEVIATES** (F20) |
| touch_outcome_tracker.py | post-response (bar ingest) | level_touch_outcomes | non-tenant | returns []/None | **DEVIATES** (F21, F28) |
| logical_trades.py | in-request | tombstones, repairs | user_id predicates present | row-drop with summary retained | **DEVIATES** (F22, F24, F29) |
| user_notifications.py | in-request + background | user_notifications | user_id on every query ✓ | returns None | **DEVIATES** (F14 — dedupe key never matches message) |
| guardrail_pipeline.py | post-response (fire-and-forget) | voice_guardrail_signals, guardrail_cues | user_id on every query; get_db service-role | suppression checks fail OPEN | **DEVIATES** (F19) |
| guardrail_context_builder.py | post-response | none (read) | user_id ✓ | returns default ctx (streak 0) | **DEVIATES** (F30) |
| gap_fill_confluence_engine.py | background loop | algo_trades (LIVE MONEY) | per-engine user_id; `update_trade_outcome` UPDATE has no user predicate | disarm is process-local | **DEVIATES** (F31) |
| option_materialization.py | post-response worker | option_materialization_jobs, position groups | sets app.current_user_id per txn; service_role only for the claim ✓ | job row records failure | **DEVIATES** (F32 — poison-pill starvation) |
| trade_gate_matrix.py | in-request | none | user_id passed to aggregates | STAND_DOWN, except vol_state | **DEVIATES** (F33) |
| verdict_v0.py | pure | none | n/a | loud refusal statuses ✓ | **DEVIATES** (F34 — n_days ≠ bootstrap n) |
| playbook_tap_matcher.py | in-request | logical_trades.playbook_id | user_id ✓ | returns (False, None) | **DEVIATES** (F35 — naive→timestamptz) |
| spotgamma_touch_detector.py | scheduler | spotgamma_level_touches | non-tenant | returns [] | **DEVIATES** (F15, F36) |
| spotgamma_email_parser.py | pure (ingest helper) | none | n/a | returns partial dict silently | **DEVIATES** (F37) |
| coach_fact_sheet.py | in-request | none | n/a | omits the line ✓ | **DEVIATES** (F38 — missing direction ⇒ "Short") |
| hiro_momentum.py | in-request | none | n/a | returns None on thin data ✓ | **DEVIATES** (F39 — spread_pct denominator floored at $1M; EMA seed double-counts) |
| rithmic_service.py | in-request | raw_trades pipeline | user_id ✓; circuit breaker + retry + timeout ✓ | drops surfaced loudly ✓ | **DEVIATES** (F40 — scrub_sensitive; gateway falls back to test) |
| strategy_level_touch_ledger.py | scheduler | strategy_level_touches | non-tenant | scan_failed swallowed | **DEVIATES** (F41 — loguru kwargs dropped) |
| legacy_calculations.py | **no live caller** | none | own psycopg2 conn, no GUC | HTTPException 500 | **DEVIATES, latent** (F42, F43) |
| apns_push.py | scheduler | apns_push_log, prunes tokens | explicit `SET app.service_role='on'` on a dedicated conn ✓ | never raises, result.skipped set ✓ | **DEVIATES** (F44 — dedup race) |
| secrets_vault.py | in-request | none | rls-skip legitimate (crypto primitive) | raises VaultNotConfiguredError / VaultDecryptError — fail-loud ✓ | conforms |
| option_strategy_economics.py | pure | none | n/a | all-None, never guesses ✓ | conforms |
| sg_substrate_ref.py | ingest | timestamp pins only | non-tenant sierra | NULL pin ✓ | conforms |
| spotgamma_client/client.py | scheduler | none | non-tenant | typed SpotGammaError/AuthError, granular timeouts, bounded retry, JWT-safe log callback ✓ | conforms |
| spotgamma_client/synth_oi.py | scheduler | synth_oi daily/chart | non-tenant | returns False, snitch suppressed ✓ | conforms (module-level `_CATCHUP_SKIP` set is unbounded but documented per-dyno) |
| equity_marketdata.py | in-request | none | conn injected; cert-gated upstream | propagates ✓ | conforms |
| trace_surface_defects.py | in-request | none | non-tenant | loud 409 refusal ✓ | conforms |
| scott_regime/cache.py | in-request | Redis only | key-scoped by caller | stale-serve is FLAGGED `served_stale` ✓ | conforms (`_singleflight_locks` dict never evicts) |
| scott_regime/mtd_placeholder.py | in-request | none | user_email echoed only | zero-filled but `degraded=True` ✓ | conforms — model for the rest of the codebase |
| flow_alignment.py | pure | none | n/a | NO_READ tri-state, never a forced call ✓ | conforms |
| spotgamma_chart_levels.py | pure | none | n/a | all-None on malformed curve ✓ | conforms |
| regime_cell_stats.py | pure + scheduler | cell stats | user_id in scheduler | wilson(0,0)=(0.5,0,1) — both call sites guard `if n` | conforms (see F43 note) |
| flow_patrol_fetch.py | scheduler | none | non-tenant | 403/404→None, 429 bounded retry, raises otherwise ✓ | conforms |
| ticker_profile_narrative.py | scheduler | spotgamma_ticker_profile.narrative_* | non-tenant | returns None ✓ | conforms (`asyncio.run` per symbol in a sync loop) |
| phonetic_rules.py | startup + in-request | none | caller supplies RLS conn; global rows only | keeps prior cache ✓ | conforms |
| trigger_phrase_detection.py | pure | none | n/a | (None,None,None) ✓ | conforms (confidence = fuzzy/100 + boost is a manufactured number, min 0.60) |
| playbook_detection_simple.py | pure | none | n/a | (None,None) ✓ | conforms (confidence floor 0.7 on any name match) |
| playbook_preprocessing.py | pure | none | n/a | returns input ✓ | conforms (logs the raw transcript at INFO) |
| zenedge_insights.py | scheduler | none (caller persists) | non-tenant | falls back to deterministic text, **labelled** `model_used="fallback"` ✓ | conforms (no timeout on the Anthropic call; exception str stored in `thinking`) |
| v12_patterns.py / v12_coaching.py | in-request / facade | patterns via memory_tools | user_id ✓ | raises RuntimeError ✓ | conforms |
| message_handler.py | background (Rithmic) | in-memory only | per-connection instance | `_parse_timestamp` → `datetime.utcnow()` on unparseable input | **DEVIATES** (F45) |
| websocket_client_protobuf.py | background transport | — | — | — | NOT AUDITED (transport wrapper adjacent to generated protobuf) |
| educator/__init__.py, scott_regime/rigor.py, spotgamma_client/__init__.py | re-export shims | none | n/a | n/a | no logic |

---
