# Appendix B — Refutation ledger

Every high-severity claim a wave-4 refuter downgraded or rejected, with the guard/context the original finder missed.
This is the audit's error record. 253 claims were adversarially re-checked; 83 did not survive as stated.

| uid | verdict | corrected | why the original claim was wrong |
|---|---|---|---|
| `1:rls-02` | OVERSTATED | trust_break | The schema fact is correct and I re-derived it: grep over all 131 db/migrations/*.sql plus the dump returns no ENABLE/FORCE/POLICY for public.users (only column-level ALTERs, e.g. 20260426113000_add_demo_flags_to_users.sql:12-13). The Stage-2 analysis is also right — 20260709140000:40-53 only visits tables that already have a GUC-referencing policy. But the stated consequence ('can SELECT auth0_id, email ... and get every account') requires a query path that does not exist: every users read I checked is explicitly  |
| `1:rls-03` | OVERSTATED | trust_break | db/schema_test_seed.sql (audit_log audit_insert) — the USING/WITH CHECK asymmetry is real and is prod-real, not seed-only: 20260419092247:93-97 DROPs and re-CREATEs the sibling audit_select, which only makes sense if both dump-visible policies exist in production. The Stage-2 argument is also correct (20260709140000:33 asserts scope exists, never that user_id matches). What the finder missed is that no code path lets a caller choose user_id — services/audit.py parameterizes it from current_user.sub. Forgery would n |
| `1:rls-08` | OVERSTATED | trust_break | Both halves shrink on re-read. (1) The open SELECT is not an oversight — db/migrations/20260425170000_create_playbook_votes.sql:51-53 states the intent verbatim above the policy: 'Vote aggregates (up_count, down_count) are inherently public social signals, so SELECT is open to any authenticated session. Writes are owner-only. Service role (empty GUC) bypasses both for backfills + admin.' The finder cited 20260619130100 but never opened the create migration that carries the design note. (2) The missing FORCE exists  |
| `1:rls-11` | OVERSTATED | trust_break | The absence of RLS is real but is an explicit, twice-documented design decision the finder quoted and then argued against: db/migrations/20260623123000_create_educator_content.sql:6-8 and again at :80-81 ('Public share links. NOT RLS — read by unguessable token on a service-role connection scoped to one token'), and routers/educator/routes_public.py:4 repeats it. I enumerated every Python reference to recap_shares and none is an unscoped read. The finder themself marked this INFERRED and said 'the capability is lat |
| `1:rls-01` | OVERSTATED | wrong_label | The mechanism is right and the counts check out, but the production half of the claim is backwards. The finder writes 'in production ... an unset connection sees and can write EVERY tenant's rows' — that was true only before db/migrations/20260709140000_rls_failclosed_stage2_cutover.sql, which adds `CREATE POLICY require_tenant_scope ... AS RESTRICTIVE FOR ALL` to every GUC-referencing table so that 'a request/job with NO scope GUC and NO service marker → restrictive is FALSE → ZERO rows (fail-CLOSED)' (migration h |
| `1:rls-02` | OVERSTATED | wrong_label | db/schema_test_seed.sql:1328 (deleted_execution_ids) — the seed genuinely lacks the RLS that db/migrations/20260619130200_rls_deleted_execution_ids.sql:27-40 adds, and the finder correctly identified why (scripts/mark_dump_satisfied_migrations.py:167-168, `satisfied = True  # pure ALTER/INDEX migration: assume in dump`). What they did not carry through is that this cuts one way only: dbmate executes that migration for real in production via Procfile `release: dbmate -d db/migrations --no-dump-schema up`. Their own  |
| `1:rls-03` | OVERSTATED | wrong_label | db/schema_test_seed.sql:7187 (playbook_votes FORCE) — same class as the entry above. db/migrations/20260619130100_force_rls_playbook_votes.sql:24 issues `ALTER TABLE public.playbook_votes FORCE ROW LEVEL SECURITY` and runs in prod through the Procfile release phase; only the hand-refreshed seed is missing it. The owner-exemption reasoning is correct as PostgreSQL semantics but applies to the CI database the finding is scoped to, not to prod. |
| `2:L3-25` | OVERSTATED | trust_break | The code facts are right: sahha_webhook.py:343-345 takes user_id from payload.external_id and :371 hands it to store_health_metrics, which writes on `async with get_db()` (database.py:406 sets service_role='on'). What the finder omitted is the reachability picture. The attacker must (a) defeat or be upstream of the Sahha HMAC, and (b) get an arbitrary external_id onto a Sahha profile — while the ZenEdge-side registration path is already hardened (routers/sahha_webhook.py:515-524 rejects a mismatched user_id with 40 |
| `2:F27` | OVERSTATED | trust_break | routers/legacy_trades.py:27 is `router = APIRouter(dependencies=[Depends(verify_user_id_ownership)])`, and utils/auth_guard.py:38-99 does the right thing: it extracts the path user_id, 400s on a non-integer, 401s when request.state.user is absent or lacks sub, resolves `SELECT id FROM users WHERE auth0_id = $1`, 403s when the DB row is missing, and 403s on `int(db_user_id) != path_user_id_int`. routers/registry/trades_registry.py:15-19 mounts the router with the dependency intact. The finder says as much ('correct  |
| `2:L1-002` | OVERSTATED | availability | The authorization gap is real: routers/algo_control.py:110-114 and :150-153 take only Depends(get_current_user), and services/gap_fill_confluence_engine.py:81-83/123-125/147-149 show SYSTEM_ARMED is a module-level global with user_id kept as an audit label only. But two parts of the stated blast radius fail. First, arming cannot 'enable REAL MONEY trading' — _execute_trade() is hard-disabled. Second, 'kill another trader's armed algo' misreads the model: there is exactly one process-global engine, not a per-trader  |
| `2:L1-006` | OVERSTATED | trust_break | The finder's own dilemma resolves against them once the policy is read. db/schema_test_seed.sql:2472 is `ALTER TABLE ONLY public.transcriptions FORCE ROW LEVEL SECURITY` and :5655 is `CREATE POLICY tenant_isolation ON public.transcriptions USING (... (user_id)::text = current_setting('app.current_user_id'))` with a matching WITH CHECK. RLS filters DELETE through the same USING clause, so `DELETE FROM transcriptions WHERE id = $1` (:708) can only remove rows already visible to this tenant. That also settles the dual |
| `2:L1-007` | OVERSTATED | trust_break | The code facts check out: routers/compat_shims.py:133-134 is `base = str(request.base_url).rstrip('/')` plus `headers = {"Authorization": request.headers.get("Authorization", "")}`, _proxy_get (:44-56) performs no host validation, and grep for TrustedHost/proxy_headers/forwarded_allow_ips across the repo, main.py's six add_middleware calls (:276-332) and the Procfile returns nothing. But the exploit as written assumes the attacker-chosen Host reaches the app, which the platform edge is designed to prevent; the find |
| `2:F-33` | OVERSTATED | trust_break | The missing check is real — routers/educator/routes.py:78-95 passes `s3_key=payload.s3_key` into edu_db.create_recording with nothing between it and the INSERT, routers/educator/models.py:23-25 declares `s3_key: str` with no validator, and routes.py:61 mints keys as `f"educator/{educator_id}/{uuid.uuid4().hex}{ext}"` so a prefix check was available. The ZEN-372 contrast is accurate. Three limits the finding does not weigh: the caller must already be an educator member (get_db_educator_dep raises 403 at database.py: |
| `2:F-27` | OVERSTATED | trust_break | Both code facts verified: routers/spotgamma_email_ingest.py:42 `x_webhook_secret: str = Header(None, alias="X-Webhook-Secret")` and :51 `if x_webhook_secret != svc.WEBHOOK_SECRET:` against services/spotgamma_email_ingest_service.py:84-86 `WEBHOOK_SECRET: str = os.getenv("SPOTGAMMA_EMAIL_SECRET", "")` with a warning that auth is 'disabled'. An explicitly-empty header does pass when the secret is unset (an absent header gives None != '' and correctly 401s). Two reductions: the bypass is env-conditional rather than un |
| `2:F-46` | OVERSTATED | trust_break | The asymmetry is real, but the function is not silently unguarded — routers/trade_import/db.py:934-941 is an explicit docstring contract: 'Tenant isolation: raw_trades enforces FORCE ROW LEVEL SECURITY, so the per-request RLS context ... already constrains this UPDATE to the caller's OWN rows'. I verified the premise rather than taking it on faith: db/schema_test_seed.sql:1947 `ALTER TABLE ONLY public.raw_trades FORCE ROW LEVEL SECURITY` and :5599 the matching tenant_isolation policy with USING and WITH CHECK on us |
| `2:F-04` | REFUTED | not_a_defect | Two things refute it. First, the response is not the raw row: routers/telegram_signals.py:519-528 constructs ActiveSignalResponse from only id, instrument, direction, created_at, expires_at, minutes_remaining and status, and models/telegram_signals.py:60-68 declares exactly those fields — telegram_user_id and telegram_chat_id are selected by get_all_active_signals but never serialized. Second, signals do not originate from arbitrary traders: routers/telegram_signals.py:61-71 restricts writers to the TELEGRAM_AUTHOR |
| `2:F-05` | OVERSTATED | trust_break | This is the same defect as 2:F-33 reported a second time from the DTO side, and it verifies identically: routers/educator/models.py:23-25 declares `s3_key: str` with no pattern or prefix constraint, and routers/educator/routes.py:84-95 passes it into create_recording with no comparison against the `educator/{educator_id}/` prefix that routes.py:61 mints. Same three reachability limits apply, so the same reduction applies. Reported twice, so it should be counted once. |
| `3:F6` | OVERSTATED | silent_loss | The hazard is real and better-evidenced than the finder knew — I found in-repo production measurement of it. But the same evidence corrects the severity. The observed production failure is direction (a): the SET lands on a different backend, require_tenant_scope rejects with 42501, and rows are silently lost (services/audit.py:132-145 fixes exactly this by wrapping in `conn.transaction()` + `set_config(..., true)`). Direction (b) — a leaked `service_role='on'` surviving on a backend and later granting a bypass to a |
| `3:F20` | OVERSTATED | trust_break | agent/trading_agent_framework_v9.py — the code facts are exactly as quoted: :1548-1550 `# For demo/local, hardcode melissa@zenedge.ai's auth0_id` / `user_id = 'auth0|67cb2449661fa91a3be894e1'`, :1553-1554 `conn = await asyncpg.connect(database_url)` with no pool and no set_config, :1585-1587 the same literal in _load_cursor_rules, and :2139-2145 `# TODO: Extract user_id from JWT token for production use`. But 'a runnable agent framework' overstates the exposure: nothing in the request path can reach it, so the risk |
| `3:F46` | OVERSTATED | trust_break | The unscoped UPDATE at services/playbook_auto_assign.py:163-174 (`WHERE id = %s AND playbook_id IS NULL`) is real, as is the missing predicate on the suggestion read at :216-220. What the finder did not check is the single caller: schedulers/match_transcriptions_v3.py:452-457 passes user_id, trade_id and transcription_id derived from the same matched row inside one per-recording loop, immediately after a read-back verification at :427-435 that the transcription's logical_trade_id equals trade_id. So the consistency |
| `3:F1` | OVERSTATED | trust_break | The code facts hold: services/price_level_alerts.py:52 `ALERT_USER_ID = os.getenv("ALERT_USER_ID", "auth0|690751db0dad7f2ecc4ecc8d")`, :645-646 `if user_id is None: user_id = ALERT_USER_ID`, and the only live caller passes nothing. But the stated consequence is wrong: 'one tenant's row lands in another tenant's notification feed and audit trail' describes data crossing between tenants, and none does — the notification content is a market level touch, and there is a single hardcoded default recipient rather than a m |
| `3:F2` | REFUTED | not_a_defect | The finding is right that the four SQL templates lack a user_id predicate, but nothing executes them. The only production importer of the module is routers/streak_metrics.py:29, which imports exactly one symbol — `from services.streak_metrics import compute_playbook_streak_analysis` — and that is the function the finder themself identifies as correctly filtered (`AND user_id = $4` at :345). The claimed harm ('compute per-user behavioural metrics over every user's trades for the instrument') cannot occur because no  |
| `1:pkfk-07` | OVERSTATED | idempotency_break | Confirmed at db/schema_test_seed_sierra.sql:1653-1677 that sierra_ohlc_5min has no PRIMARY KEY and that uq_5m_spine (line 3739) covers (instrument, contract, timestamp) with only `contract` NOT NULL. But the finder's consequence — unbounded duplicate bars and double-counted volume/ATR — requires a NULL instrument or timestamp, which the sole production importer cannot produce. What survives is a latent hygiene/idempotency gap (a manual \copy or a future writer could bypass ON CONFLICT), not active corruption. |
| `1:sierra-11` | OVERSTATED | loud_error | The mechanism is real — DROP SERVER sierra_srv CASCADE (scripts/setup_fdw_from_env.py:44) destroys all three foreign tables on that server (db/schema_test_seed.sql:3264, 3352, 3405) while IMPORT FOREIGN SCHEMA ... LIMIT TO (sierra_ohlc_1min) (:68-73) recreates only one. But the consequence is not silent. compute_excursions_1m's STEP 1 reads sierra_chart_remote.tws_ohlc_1min (db/schema_test_seed.sql:212-221); a missing relation raises inside plpgsql and propagates to the callers (worker.py:971, routers/trade_import/ |
| `1:sierra-18` | OVERSTATED | silent_loss | The code defect is exactly as written — schedulers/ibb_evaluate.py:96-105 selects `FROM sierra_chart.sierra_ohlc_1s WHERE timestamp > $1` with no instrument or contract predicate, and `symbol` fetched at :64 is never used in that query. But with zero rows returned, `bars` is empty, the WIN/LOSS loop never runs, the EXPIRED branch is guarded by `if outcome is None and bars:` (line 141), so nothing is written. Today this produces no wrong labels — it produces no evaluations at all. The finder acknowledged this maskin |
| `1:eval-01` | OVERSTATED | silent_loss | Verified schedulers/ibb_evaluate.py:64-72 selects `symbol` and :96-102 ignores it, and schedulers/zdpsb_evaluate.py has the identical shape. The 'grades each signal against bars from every instrument in the table' consequence requires the table to hold bars; it holds none. Unlike 1:sierra-18, this entry does not disclose the masking, which is the material overstatement. Note also that these are duplicates of each other and should not be published twice. |
| `1:rp-01` | OVERSTATED | silent_corruption | The '10x divergence between three sources of truth' framing conflates two different tables. db/schema_test_seed.sql:2503's DEFAULT 25000.00 is on public.user_account_risk (per-broker-account settings); the two Python 2500.00 values (services/risk_resolver.py:61-63 and routers/user_risk.py:157) both target public.user_risk_profiles, whose account_size is `numeric(14,2) NOT NULL` with NO default (db/schema_test_seed.sql:2715). Those two agree with each other. What is real is the second half: services/risk_resolver.py |
| `1:tz-01` | REFUTED | not_a_defect | The stated mechanism — 'binding a naive datetime to timestamptz makes the value depend on the session TimeZone rather than on the datetime' — does not hold for this driver. routers/bookmap_stats_import.py:444-453 binds via `conn.fetchval` (asyncpg), and asyncpg's timestamptz codec converts client-side: a naive datetime is encoded as UTC regardless of the server's TimeZone setting. Since entry_time_naive is literally `entry_time_utc.replace(tzinfo=None)` (:317-319), the two columns denote the same instant by constru |
| `2:L3-11` | OVERSTATED | silent_corruption | The write is real: routers/rithmic_import.py:1174-1214 builds two Trade objects and calls `process_trades_for_date(conn, synthetic_trades, trade_date, user_id)` on the caller's RLS connection with no dry-run branch and no cleanup, and the verification block at :1217-1244 counts what it just created. But 'writes two fabricated ES fills into the caller's real trade tables' applies only to a whitelisted beta user on a flag-enabled deploy, not to users generally. |
| `2:L3-14` | REFUTED | not_a_defect | The finding assumes sierra_chart.spotgamma_daily_levels holds index-scaled values. services/market_state.py:299-303 records the opposite from a real incident: 'observed 2026-06-05: ES touch close 7493 vs ZG 7495, but entry recorded as index 7464' — the zero_gamma read from that same table (services/market_state.py:497-507 `SELECT zero_gamma, call_wall, put_wall, vol_trig, combo_l1..l4 FROM sierra_chart.spotgamma_daily_levels WHERE symbol = $2`) sat 2 points from the ES future and 31 points from the index. The same  |
| `2:L3-26` | OVERSTATED | loud_error | The missing transaction is real — routers/internal_glossary.py:52-82 is a bare `for r in payload.rules:` with one `await conn.execute(...)` per rule under asyncpg autocommit, no `async with conn.transaction()`, no per-rule try/except, and `phonetic_rules.refresh_global()` at :84 sits outside the try so it is skipped when the loop raises. But the failure is loud (the exception propagates to a 500), and every write is an idempotent `ON CONFLICT ... DO UPDATE` upsert, so a re-POST of the whole batch converges rather t |
| `2:F04` | REFUTED | silent_loss | public.video_requests.user_id is `character varying(255) NOT NULL` (db/schema_test_seed.sql:3039), and the table carries FORCE RLS (:3042) + ENABLE RLS (:5851) with a WITH CHECK requiring `(user_id)::text = current_setting('app.current_user_id', true)` (:5739). Binding the integer `surrogate_id` (routers/notifications.py:109) as $1 through asyncpg raises a type error before the statement executes; even if it were coerced, the RLS WITH CHECK would reject a hash string that is not the caller's auth0 sub. So rows are  |
| `2:F18` | OVERSTATED | silent_loss | Confirmed the silent-loss half: quantity<=0 (`continue` at :337-342), unparseable price (:343-352) and any per-row exception (:405-419) drop rows that never appear in the response, while the success path returns `errors=rebuild_errors if rebuild_errors else []` with `message = f"Successfully processed {new_raw_count} new trades..."` (:855-870, :911-918), and account truncation to 20 chars at :393-395 is real. But two sub-claims are softened: unbalanced groups ARE surfaced to the client via `dropped_suffix` appended |
| `2:F21b` | OVERSTATED | misleading_message | The missing age predicate is real — routers/trace/trace_monitor.py:141-158 is `ORDER BY timestamp_utc DESC LIMIT 1` with no staleness filter, while /health computes age_minutes and calls >15min 'stale' (:332-340) and answers a failed check with HTTP 200 `{"status": "error", "message": str(e), ...}` (:347-348). But the claim that staleness 'is invisible on the surface people actually read' is wrong: /current returns the row's own timestamp as `collected_at=collected_at` in the TraceSnapshot (:222-238), so a client c |
| `2:F23b` | OVERSTATED | loud_error | The source lines are as quoted — `pnl_ticks = int((outcome_price - entry_price) * 4)` at :911 with no instrument lookup, and `bars = [b for b in instrument_bars if b[3] > signal_time][:500]` at :873. But the stated consequence ('stored as the row's pnl_ticks, feeding avg_pnl_ticks in /stats') never occurs, because execution aborts at the batch bar query before any signal is graded. This is a broken endpoint, not silent corruption. |
| `2:L1-010` | OVERSTATED | silent_loss | The `else: logger.warning(...); buy_sell = "B"` at :385-388 is exactly as quoted, but reaching it requires an Action string beginning with neither B nor S, which the finder can only motivate hypothetically ('a localised export'). What is solidly real at the same site is silent loss: quantity<=0 `continue` (:392-395), zero/missing price `continue` (:396-400) and the per-row `except ... continue` (:445-449) drop fills that never surface, while the success path returns `errors=[]` with a 'Successfully processed' messa |
| `2:L1-012` | OVERSTATED | misleading_message | The blanket `except Exception` at routers/ibb_walkforward.py:399-414 returning HTTP 200 with win_rate=74/51/65 is exactly as quoted, and data_source='BASELINE' is indeed the same discriminator the healthy-sparse path returns (:357). But 'fabricated' overstates it: each of those WalkForwardSummary rows carries `description="Historical baseline"` in the payload, and evaluated_signals=0 / total_signals=0 distinguish the failure from a populated live response. The genuine defect is that a database failure is served as  |
| `2:L1-021` | REFUTED | not_a_defect | The finder assumed an unknown external contract. `base = str(request.base_url).rstrip('/')` (routers/compat_shims.py:447, :583) means _proxy_excursions self-proxies to /api/sierra2/calculate-trade-excursions-v2 in this repo — routers/sierra_marketdata.py:652-686, which returns `results` straight from services/sierra_marketdata.py::excursions_core. Reading excursions_core end to end (services/sierra_marketdata.py:547-703), it is a single `for trade in trades:` loop in which every branch appends exactly one result be |
| `2:L1-022` | REFUTED | not_a_defect | The finder's claim that 'the write path accepts the zero' is wrong. The upsert endpoint's DTO types those fields as constrained: models/user_risk.py:42-44 `account_balance: PositiveFloat`, `risk_percent: PositivePercent`, `prop_multiplier: PositiveFloat`, and models/_types.py:135-137 define `PositiveFloat = Annotated[float, Field(gt=0)]` and `PositivePercent = Annotated[float, Field(gt=0, le=100)]`. A 0 gets a 422 before the handler runs. The only other writer to user_risk_profiles is the auto-create at routers/use |
| `2:F-3` | OVERSTATED | silent_loss | The nondeterminism is real: `SELECT DISTINCT source FROM raw_trades WHERE user_id = $1 AND instrument = $2 LIMIT 1` (rebuild.py:82-92) returns an arbitrary member of the distinct set, and past it the destructive block is scoped by user_id+instrument with no source predicate — the raw unlink at :206-217, the transcription orphaning at :225-236 and the DELETE at :242-252. One mitigation the finding omits: the DELETE is further constrained to `id = ANY($3::uuid[])` where the array is raw_linked_ids, so direct-write lo |
| `2:F-37` | OVERSTATED | misleading_message | The 200-on-failure is real — routers/level_edge.py:100-113 catches everything and returns data_source='BASELINE' with zeros. But the framing 'answers a database failure with backtest marketing numbers' misreads the payload shape: `baseline=BASELINE_DATA` is ALSO returned on the healthy path (services/level_edge_service.py:387 `"baseline": BASELINE_DATA`), so the 66.7%/+2.34R block is not substituted during an outage — it is always there. The surviving defect is that an internal failure is indistinguishable from a b |
| `2:F-18` | REFUTED | not_a_defect | The finder read the Field description and missed the validator eight lines below it. models/playbook.py:43-59 defines `@field_validator("playbook_id") def _validate_assignable(cls, v)` which raises ValueError unless v is None or v in ASSIGNABLE_PLAYBOOK_IDS, where ASSIGNABLE_PLAYBOOK_IDS = range(1,41) | {50..54} | range(60,66) | {90,91} (models/playbook.py:24-30). The docstring even explains why an explicit set was chosen over ge/le ('Widening the old ge=1, le=40 bound to a range would also expose the 41-49, 55-59  |
| `2:F-24` | OVERSTATED | silent_corruption | Confirmed routers/trade_import/parsers.py:144 `result = tick_sizes.get(instrument)` uses the raw argument rather than the `_sym = instrument.upper().strip()` computed at :106, and :150-153 falls through to `return Decimal("0.25")`. But the case-sensitivity half is neutralised: services/instrument_registry.py:579-591 `def get(self, symbol)` begins `upper = symbol.upper()`, and MYM is registered (instrument_registry.py:166), so `get_tick_size_for("mym")` misses the dict, hits the registry fallback and returns the cor |
| `2:L6-42` | OVERSTATED | trust_break | The encoding asymmetry is real and the sibling documents the rule: services/educator/zoom_client.py:96-98 applies `enc = quote(quote(meeting_uuid, safe=""), safe="")` with the comment "UUIDs with '/' or '//' must be double-URL-encoded per Zoom's API rules", while background.py:170 interpolates external_ref raw. But the claimed consequence overstates it: the object is NOT unfindable — the very next line (:171) persists the identical key via set_recording_audio_key, and an S3 prefix rule on `educator/{id}/zoom/` stil |
| `2:L6-45` | OVERSTATED | misleading_message | The code is as quoted — `gating_threshold = float(os.environ.get("V3_GATING_THRESHOLD", "0.50"))` at :266 and again at :569, with no try/except, no clamping, and the comment at :265 showing the per-request read is deliberate ('Read threshold at call time (not import time)'), presumably so it can be retuned without a redeploy. The single defect worth reporting is that /status reports the parsed value as the live threshold while a 0 would have silently turned the gate into a pass-through; that is a config-hardening a |
| `3:F4` | OVERSTATED | silent_corruption | The direction of the bug is confirmed: services/sierra_marketdata.py:1436-1439 is `ORDER BY trade_date ASC LIMIT $2` inside a CTE whose window is `timestamp >= NOW() - ($3 * INTERVAL '1 day')` with $3 = lookback_days + 10 (:1444) and $2 = lookback_days (:1443), so it keeps the OLDEST N sessions of the window rather than the newest. But the magnitude claim is wrong: the +10 is ten CALENDAR days, which contains roughly 7 trading sessions, and the HAVING COUNT(*) >= 50 filter discards thin days. For lookback_days=14 t |
| `3:F5` | OVERSTATED | misleading_message | The hardcoded fallback is exactly as quoted — services/sierra_marketdata.py:1132 `contract = resolution.get("chosen_contract") or resolution.get("contract") or f"{root}Z25"` — and the bar query at :1145-1157 filters `WHERE contract = $1` against it. But the response is not as opaque as claimed: the zero-row branch at :1157-1172 returns `bar_count: 0` alongside `ib_high: 0, ib_low: 0, ib_range: 0, is_complete: False`, so a caller has an unambiguous tell that nothing was measured. The defect is a stale magic constant |
| `3:F13` | OVERSTATED | silent_corruption | The silent constant substitution is real (hiro_grading_engine.py:136-138), but the stated consequence — 'a trader is handed a stop and a size derived from a number that was never measured', 'no provenance' — does not hold for the trader-facing path. The live sizing endpoint has its own fetch that returns a third element and surfaces it: routers/hiro/position_sizing.py:102/131 `return fallback, atr_instrument, True`, rendered as `is_fallback=is_fallback` at :281 into ATRDetails.is_fallback (models/hiro_position_sizi |
| `3:F14` | OVERSTATED | silent_corruption | The headline (stop_ticks 0 → stop_price == entry_price, size forced to 1) requires atr_points == 0 or tick_size <= 0. Neither can occur: get_atr_at_entry guards `if row and row.atr` (:131), so a 0/NULL ATR falls to FALLBACK_ATR_POINTS whose smallest value is 0.50 and whose default is 10.0 (:97, :136); tick_size comes from utils/tick_sizes.get_tick_size, which returns 0.01 for OCC/equities and 0.25 as the documented 'safe default' (utils/tick_sizes.py:80,99,105) — never 0. What survives is only the `int()` truncatio |
| `3:F29` | OVERSTATED | silent_corruption | The stated harm — 'a 10-lot with a NULL total_quantity reports its full position P&L as per-contract, a 10x overstatement' — is impossible: public.logical_trades.total_quantity is `integer DEFAULT 0 NOT NULL` (db/schema_test_seed.sql:1648), and both feeding queries select that column directly (services/scott_regime/mtd_telemetry.py:213-214, :230). The `or 1.0` at :74 can therefore only fire on a total_quantity=0 row, a degenerate case whose pnl_ticks is meaningless anyway. The defensive-coding smell is real; the 10 |
| `3:F31` | REFUTED | not_a_defect | The numerator/denominator mismatch needs a falsy entry in first_times. There cannot be one: first_times is populated only from DaySession.first_trade_time (services/v12_daily_brief.py:152, agent/v12/day_of_week_report.py:2024), sessions are built exclusively by analyze_session, and analyze_session returns None when `if not trades` (agent/v12/day_of_week_report.py:919-920) with the caller filtering `if _s is not None` (services/v12_daily_brief.py:70-71). Every surviving session has >= 1 trade, so `first_time = trade |
| `3:F33` | OVERSTATED | silent_corruption | The missing `not isinstance(value, str)` guard at services/regime_vocab.py:97-98 (present at :124-125) is real. But the harm needs `conditioning.regime_filters.vix_bucket` to be a bare string in the DB. The sole production writer is services/user_playbook_service.create_playbook, which dumps a validated PlaybookPrimitive (`conditioning_json = json.dumps(primitive.model_dump(mode="json"))`, :268-274), and models/user_playbook.py:151-154 types those fields `list[GammaRegime]/list[TodWindow]/list[VolState]/list[VixBuc |
| `3:L4-18` | OVERSTATED | trust_break | Half (b) is confirmed verbatim: schedulers/rebuild_rithmic_groupings.py:643-651 pings the snitch and returns on both ENABLE_DELAYED_GROUPING=false and ENABLE_RITHMIC_STATELESS_REBUILD=false, so DMS reports green while the whole subsystem is off. Half (a) is not supported. The import holds a per-user advisory lock (worker.py:573-580) and — decisively — the rebuild is account-isolated: it groups by user_id + instrument + account (schedulers/rebuild_rithmic_groupings.py:132-146, :178) and passes `account` into the fla |
| `3:F10` | REFUTED | not_a_defect | utils/currency_calculations.py:34-36 does return 0.0 on KeyError as described, but nothing in the codebase imports the module or calls either function — no router, no service, no scheduler, not even a test. The claimed harm ('the caller cannot distinguish break-even from unknown') has no caller. The genuinely live tick-value divergence the finding gestures at (logical_trades.py:903/938, utils/instruments.py:81) is a different site. |
| `3:F5` | REFUTED | not_a_defect | The `+1` only skips a bar on the fallback branch, and the fallback cannot be taken by any caller. Every call passes the SAME bars list that produced the signals — backtest/gsst_primary.py:279 (detect_touches(bars) then evaluate_signals_batch(self._gsst_signals, bars)), :370-385, and :432-448 — and the detector sets `signal_timestamp=timestamp` where `timestamp = current_bar.get('timestamp')` (backtest/gsst_detector.py:613, 677, 726). So `signal.signal_timestamp in bar_index` (gsst_evaluator.py:190) always hits and  |
| `3:F7` | REFUTED | not_a_defect | sierra_chart.ludwig_levels was dropped as a verified ghost (0 live rows, 0 index scans, 0 code references) by db/migrations_sierra/20260302080200_sierra_ghost_table_cleanup.sql:20, with no re-create anywhere in db/migrations_sierra; routers/sierra.py:235 states it outright — 'DISABLED: ludwig_levels table does not exist yet' — with the reader endpoints commented out. So the INSERT at sierra/ludwig_import_pipeline.py:401/423 cannot land rows and the claimed 7-8h skew 'relative to menthorq_v2/sierra_ohlc_* in the sam |
| `3:F21` | OVERSTATED | silent_corruption | The field-probing precedence in the live microservice is real (rithmic-microservice/app_protobuf_v4.py:173, :176 — order_qty before filled_qty, price/limit_price before fill_price/exec_price) and those rows do become the `orders` payload the backend imports (:361 → :608 → :621, consumed at routers/rithmic_import.py:589-611). But the claimed second decoder is not live: services/rithmic_protocol/message_handler.py is imported only by rithmic-microservice/app.py:18 (which the Dockerfile does NOT run — CMD is app_proto |
| `3:F11` | REFUTED | not_a_defect | The EITHER logic really is written as described (services/gamma_touch_evaluator.py:509-511 `adverse = 0  # Can't fail with EITHER direction`, :531 `and expected_dir != "EITHER"`, :545 `if max_favorable >= max_adverse or expected_dir == "EITHER"`), but no touch can be recorded or resolved: every touch read/write in the module targets the dropped tables (:387, :437, :477, :565, :579, :618, :631, :644, :678). The scheduler swallows the resulting per-instrument exception (schedulers/gamma_touch_evaluate.py:81-83) and s |
| `3:F12` | REFUTED | not_a_defect | The hard far edge is genuinely in the code (services/gamma_touch_evaluator.py:403-412: support requires `bar.low <= price + threshold AND bar.low >= price - threshold * 2`, i.e. ES [price-1.00, price+0.50]), and the EITHER branch at :415-417 is even tighter (symmetric ±threshold). But the sample it would bias is never persisted: the dedupe SELECT at :387 and the INSERT at :437 both hit sierra_chart.gamma_level_touches, dropped by db/migrations_sierra/20260302080200:27. No touches, no bounce_rate, no downstream cond |
| `3:F24` | OVERSTATED | silent_corruption | The fallback is real (services/firing_detector.py:398-400 `closes = state.recent_closes or [state.price]`) and it does compare one spot sample against the wide backtest threshold (ES 2.0 / NQ 8.0, :74). But two of the finding's supports fail. It is documented, not accidental: services/market_state.py:552 states 'Empty (no recent rows / stale feed) → caller falls back to [spot]; never raises', and the field comment at :118-120 repeats it. And the claim that 'the emitted sg_rulebook_firings row records no marker' is  |
| `3:F26` | OVERSTATED | wrong_label | The three 1.0 returns are exactly where the finding says (services/historical_regime.py:146-147, 158-159, 161-162) and they do feed `vol_state = _classify_vol_state(vol_ratio)` at :206 with no measured/defaulted distinction in the returned dict. But this is a deliberate, documented design choice, not an oversight: the function docstring at :128-130 states 'Returns 1.0 (→ NORMAL) on insufficient history — the conservative default (only P5 filters on vol_state, and it allows NORMAL)'. The blast radius is correspondin |
| `3:F5` | REFUTED | not_a_defect | The lower-bound-only bar query (services/spotgamma_signal_evaluator.py:310-317) and the barrier-walk-before-session-check ordering (:233-273) are exactly as described, so the logic critique is fair — but no signal can be evaluated: the table the evaluator SELECTs and UPDATEs was dropped as a verified ghost (0 live rows, 0 code references) and never re-created, and no scheduler, router or worker instantiates the class. No WIN/LOSS is ever written by this path. |
| `3:F6` | OVERSTATED | silent_corruption | The padding is real and never trimmed (services/sierra_data.py:911-913, passed to _fetch_ohlc_rows at :998-1006/:1013-1021, iterated whole at :1067, and the 'sanity check' recomputes from the same rows at :1086-1098). But the blast radius is wrong: this is the superseded path. The router itself says '[LEGACY - Use compat_shims version instead]' (:250-255), and the live excursion path (routers/sierra_marketdata.py:668/732 → services/sierra_marketdata.excursions_core) DOES trim back to the trade window — `window_bars |
| `3:F13` | REFUTED | not_a_defect | Even granting the module were live, the 0.25 default cannot fire: the only producer of spotgamma_signals writes ES, NQ and RTY only — services/spotgamma_signal_recorder.py:246 `for instrument in ["ES", "NQ", "RTY"]` — which is exactly TICK_SIZE_MAP's key set (services/spotgamma_signal_evaluator.py:50-54). There is no CL or GC signal to misprice. The residual sub-claims (int() truncation at :372-374, EXPIRED storing pnl_ticks = 0 at :277-279) are real as code but sit on the same dead path. |
| `3:F21` | REFUTED | not_a_defect | The numerator cannot exceed the denominator. The only writer of hit_mfe_10_first is the DB function public.check_level_touch_outcome, which sets `hit_mfe_10_first = CASE WHEN v_hit_mfe THEN TRUE ...` and `tracking_status = CASE WHEN v_outcome_hit THEN 'completed' ...` in the SAME UPDATE (db/schema_test_seed.sql:944-947) — a flagged row is always 'completed'. The only other status transition, expire_tracking_for_date, is gated `AND tracking_status = 'in_progress'` (:509-514), so a completed row can never become 'exp |
| `3:F23` | REFUTED | not_a_defect | Beyond reachability, the corruption claim does not hold on the deployed host. asyncpg encodes a naive datetime for timestamptz via astimezone(), which resolves against the process TZ; production runs TZ=UTC (the repo states the convention at tests/realdb/test_ninja_orphan_regrouping.py:63), so the age gate and cutoff bind correctly. The 'two outcome classes not comparable' half is also wrong: _get_session_end_time explicitly converts to UTC before stripping tzinfo (services/spotgamma_signal_evaluator.py:341-356), s |
| `3:F35` | OVERSTATED | silent_corruption | The comment's premise is indeed false — public.logical_trades.entry_time is `timestamp with time zone NOT NULL` (db/schema_test_seed.sql:1646), so the strip at services/playbook_tap_matcher.py:86-90 is unnecessary and the stated rationale ('asyncpg rejects aware datetimes') is wrong. But the consequence does not follow in production: asyncpg's astimezone() on a naive value resolves against the process TZ, and prod/CI run TZ=UTC (tests/realdb/test_ninja_orphan_regrouping.py:63), where naive == the intended UTC insta |
| `3:F45` | REFUTED | not_a_defect | _parse_timestamp does return datetime.utcnow() on both the empty and the unparseable path (services/rithmic_protocol/message_handler.py:157-158, :178-179), but no imported fill ever passes through it. The deployed microservice entrypoint is app_protobuf_v4 (rithmic-microservice/Dockerfile:13 `CMD ["uvicorn", "app_protobuf_v4:app", ...]`), not app.py; nothing outside the package imports trade_collector (`grep -rn 'RithmicTradeCollector|trade_collector'` returns only routers/rithmic_import.py:428, an os.path.exists() |
| `1:idx-11` | OVERSTATED | loud_error | Half the finding is refuted. The 3-column constraint is NOT dead weight: it is the load-bearing ON CONFLICT arbiter for both writers — agent/v12/coaching/persistence.py:80 `ON CONFLICT (user_id, session_date, instrument)` and agent/v12/memory/persistence.py:148, same clause. Without it those upserts would fail to plan. What is real is the opposite failure: because the arbiter is the 3-col index, a second-instrument row on the same date is NOT absorbed by DO UPDATE, so it hits uq_coaching_session (user_id, session_d |
| `1:pkfk-03` | OVERSTATED | not_a_defect | The FK is exactly as quoted (db/schema_test_seed.sql:5241-5242, ON DELETE CASCADE onto a table with no user_id at :2406-2421), so the schema hazard is real. But the finder's trigger — 'an operator pruning stale zones, a detector reset, a cleanup script' — is not supported by any code in the repo; the one cleanup script it cites only matches test zone_ids. As written today this is a latent schema footgun with no reachable production trigger, not an active silent-loss defect. It belongs in a hardening list (RESTRICT  |
| `1:pkfk-04` | OVERSTATED | idempotency_break | The key shape is real (db/schema_test_seed.sql:3622 PRIMARY KEY (user_id, execution_id); table at :1328-1334 has no account column) and the four checks match on (user_id, execution_id) only. What is overstated is the collision evidence. The finder's only proof that broker execution IDs repeat across accounts within one user is db/migrations/20260616160000:11-13, which names tastytrade specifically — and there is no tastytrade uploader in the repo (`ls routers | grep -i tasty` is empty; the tombstone-checking upload |
| `1:pkfk-15` | OVERSTATED | silent_loss | The DDL is exactly as quoted (db/schema_test_seed.sql:7246-7251, instrument_symbol TEXT NOT NULL REFERENCES public.instruments(symbol) ON DELETE CASCADE) and the cascade would silently drop membership rows for every user's playbook. The defect kind is right. The blast-radius framing is wrong: 'retiring an instrument from the global registry' is described as 'a routine registry-maintenance action', and it is not — the only in-repo deleter is a revert path that the migration's own header (:139-144) already flags as a |
| `1:sierra-08` | OVERSTATED | silent_loss | The finder missed the matview's own rolling window, which is the guard. db/migrations_sierra/20260424053000:67 bounds the v4 slab with `WHERE h.minute_utc >= now() - INTERVAL '100 days'` and :78 bounds the live slab with `AND rho.snapshot_ts >= now() - INTERVAL '100 days'`. mv_regime_minute_snapshot is BY DESIGN a rolling ~100-day window, not a growing archive — so the claimed '~37 days of history that a REFRESH silently drops, widening by one day per day' is wrong: everything before now()-100d is already outside t |
| `1:co-01` | OVERSTATED | trust_break | The 'no P&L path reads it' half holds — I re-ran the grep: outside routers/user_trading_preferences.py the only commission arithmetic in the repo is agent/trading_agent_framework_v4.py:491 and _v5.py:491, both `def analyze_cost_impact(trades, commission_per_tick: float = 0.82)`, a hardcoded constant that never consults user_trading_preferences. So a user's override changes no displayed number. But 'silent_loss' is the wrong label: the value is persisted (db/schema_test_seed.sql:2754), parsed back on read (routers/u |
| `1:sierra-02` | OVERSTATED | not_a_defect | The mechanism checks out — scripts/mark_dump_satisfied_migrations.py:158-160 sets `satisfied = True` for any migration with no CREATE TABLE ('pure ALTER/INDEX migration: assume in dump') and inserts the version into schema_migrations without running it, so 20260329000100's INSERT never executes against the test DB. But the stated consequence is refuted by reading the functions themselves. The finder claims sierra_meta.get_contract_at and get_roll_windows_at 'read that table'; they do not. db/schema_test_seed_sierra |
| `1:down-04` | OVERSTATED | loud_error | Defect (1) is real: db/migrations/20260226080000_drop_ghost_recall_frames_and_functions.sql:28 does `DROP TABLE IF EXISTS _ghost_recall_frames_pre_partition CASCADE`, so 20260223060300's down at :19-20 will always fail on the second rename with 42P01. But that migration carries no `transaction:false`, so dbmate wraps it — the first `ALTER TABLE recall_frames RENAME TO recall_frames_new` is rolled back atomically. The finder concedes this ('dbmate's transaction saves it') and then builds defect (2) on the premise 'i |
| `2:L3-13` | OVERSTATED | not_a_defect | The code shape is exactly as described and I confirm it: routers/ninja_agent_upload.py:554 `all_logical_trade_ids = list(logical_trade_ids) if logical_trade_ids else []` sits directly under the comment 'Collect IDs from both process_trades_for_date and stateless_rebuild_instrument', while the rebuild's `logical_ids` (:475) reaches only the log line at :487. routers/ibkr_agent_upload.py:418/434 does collect `rebuild_logical_ids`, threads them to run_option_grouping_second_pass at :469, then drops them at :507 — the  |
| `2:F-5` | OVERSTATED | misleading_message | The code fact is exact — accept (:210-219) and reject (:410-419) run byte-identical `UPDATE transcriptions SET suggested_playbook_id = NULL, playbook_confidence = NULL WHERE id = $1 AND user_id = $2`, differing only in a comment. But the finder frames 'accepting assigns nothing' as a hidden defect, and it is not hidden: the handler's own docstring at :150-151 states it outright — 'This endpoint clears the suggestion fields from the recording after user acceptance. Frontend can then navigate to manual playbook assig |
| `3:L4-05` | OVERSTATED | loud_error | The functional half is correct and I confirm it: schedulers/level_edge_evaluate.py:113-119 selects `high, low, close, minute_utc FROM sierra_chart.sierra_ohlc_1s`, and that table (db/schema_test_seed_sierra.sql:1610-1626) has `"timestamp"`, not `minute_utc` — so asyncpg raises UndefinedColumnError and evaluate_pending_signals has only a `finally: await conn.close()` at :227-228, no except. No Level Edge signal resolves. But 'silent' is refuted by the entry point the finder did not follow. The module ends with `asyn |
| `3:F17` | OVERSTATED | silent_loss | legacy_trades_service.py — the all-instruments P&L list. The defect is real: the try/except at :339-360 wraps the whole per-instrument block including the inner `for g in groupings` loop, so the first None-priced grouping aborts that instrument and `continue`s. But the title overstates the loss. `result.append(...)` happens incrementally inside the inner loop (:347-357), so every grouping processed before the failure is retained — what is lost is the remainder of that instrument's groupings, not the entire instrume |
| `1:rls-09` | OVERSTATED | idempotency_break | The factual core checks out: db/migrations/20260226070000:18-20 excludes idempotency_keys, deleted_execution_ids and rithmic_fill_retry_queue by name, and grepping the seed for ENABLE/FORCE ROW LEVEL SECURITY or CREATE POLICY on any of the four returns nothing (deleted_execution_ids does get RLS in prod via db/migrations/20260619130200, as the finder concedes — the seed is the drift). But the stated consequence — 'any authenticated connection can INSERT a row for (some_key, victim_user_id)' — conflates a DB-role ca |
| `2:L1-027` | OVERSTATED | not_a_defect | Both named bypasses are refuted by reading the implementations. (1) Idempotency: the middleware's lookup at middleware/idempotency.py:172-177 is `WHERE idempotency_key = $1 AND user_id = $2` — path is stored but never compared (that is this set's own finding 3:F2). So re-issuing the same key at the second prefix is treated as a REPLAY, not as a fresh caller — the opposite of the claim. (2) Rate limiting: utils/rate_limit.py:82 builds the counter key as `f"ratelimit:{ip}:{max_calls}:{window}:{window_start}"` — no pa |
| `3:F29` | OVERSTATED | idempotency_break | The swallow is exactly as quoted (:141-149, `except Exception as e: logger.warning(f"[TRADES] Tombstone creation failed ..., deletion will continue: {e}"); return 0`) and the finder's argument about fail-open direction is a fair design critique. But the headline consequence — 'the delete proceeds, so deleted trades re-import on the next sync' — holds only on the two paths that return 0 WITHOUT raising: ENABLE_USER_DELETE_TOMBSTONE=false (:88-90, documented intent) and the information_schema table-missing check retu |